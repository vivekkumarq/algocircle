import { QuestionSet } from './question.model';

export const TWO_POINTERS_QUESTIONS: QuestionSet = {
  topic: 'two-pointers',
  questions: [
    { level: 'Easy', q: 'What makes a problem a two-pointer problem?', a: 'A monotonic relation, so moving a pointer always pushes the value the same way. Sorted input, a pair or triplet target, an `O(1)` space requirement, or the words "in place", "from both ends", "remove" or "partition".' },
    { level: 'Medium', q: 'Why is it valid to move `lo` when the sum is too small?', a: 'If `a[lo] + a[hi]` is already below the target, then `a[lo]` paired with anything at or below `hi` is smaller still — so `a[lo]` cannot be part of any solution and is discarded forever. That argument is the technique.' },
    { level: 'Easy', q: 'What are the two families of two-pointer movement?', a: 'Opposite ends moving inward, which needs sorted data or symmetry; and same-direction, where a follower marks where output is written and neither pointer rewinds.' },
    { level: 'Medium', q: 'Container with most water — what is the greedy move?', a: 'Move whichever side has the shorter wall. Keeping it and shrinking the width can only reduce the area, so the shorter side cannot be part of a better pair. `O(n)`.' },
    { level: 'Medium', q: 'Three-sum: where do candidates usually go wrong?', a: 'Duplicate handling. Skip a repeated anchor, and only skip repeated values on both sides **after** recording a hit — skipping before it drops valid triplets.' },
    { level: 'Medium', q: 'Three-sum closest — how does it differ?', a: 'Same scan, but instead of stopping at an exact match you track the smallest absolute difference seen and keep moving whichever pointer pushes the sum toward the target.' },
    { level: 'Medium', q: 'Four-sum — how do you extend the pattern?', a: 'Two nested loops fixing the first two values, then two pointers on the suffix, giving `O(n³)`. Prune early when the smallest or largest achievable sum already misses the target.' },
    { level: 'Medium', q: 'How do you merge two sorted arrays in place from the back?', a: 'Start both read pointers at the last real elements and write from the end of the larger array. Writing backwards guarantees you never overwrite a value you still need to read.' },
    { level: 'Medium', q: 'How do you partition an array around a pivot?', a: 'Lomuto: a write pointer marks the boundary of the smaller region; scan and swap anything below the pivot into it, then place the pivot at the boundary. This is quick sort\'s inner step.' },
    { level: 'Medium', q: 'Explain fast and slow pointers.', a: 'Two pointers moving at different speeds. When the fast one has moved `2k` steps the slow one has moved `k`, which gives you the middle, the nth from the end, and cycle detection without knowing the length.' },
    { level: 'Hard', q: 'Why must fast and slow pointers meet inside a cycle?', a: 'Once both are in the cycle the gap between them changes by exactly one each step, so it cannot jump over zero. It must eventually become zero.' },
    { level: 'Hard', q: 'After they meet, how do you find where the cycle starts?', a: 'Reset one pointer to the head and advance both one step at a time; they meet at the entry. The distance from head to entry equals the distance from the meeting point to the entry going forward.' },
    { level: 'Medium', q: 'Squares of a sorted array with negatives — how do you keep it linear?', a: 'The largest square is at one of the two ends, so fill the output from the back using two pointers moving inward. Sorting again would be `O(n log n)`.' },
    { level: 'Medium', q: 'Is a subsequence check a two-pointer problem?', a: 'Yes — advance the pointer into the candidate only when characters match, and always advance the pointer into the text. If the candidate pointer reaches its end, it is a subsequence. `O(n)`.' },
    { level: 'Medium', q: 'When do two pointers stop working?', a: 'When the relation is not monotonic — a product target with negatives, or when sorting would destroy index order the answer depends on. Then hashing or another structure is correct.' },
    { level: 'Medium', q: 'Trapping rain water with two pointers — why is the smaller side safe?', a: 'Water above a bar is `min(leftMax, rightMax) - height`. Whichever max is smaller is the binding constraint, so that side can be resolved immediately and moved.' },
    { level: 'Easy', q: 'How do you remove a given value in place and return the new length?', a: 'A write pointer: copy forward every element that is not the target. Everything before `write` is the answer and the tail is stale, which the problem permits.' },
    { level: 'Medium', q: 'Remove duplicates allowing at most two of each.', a: 'Same write pointer, but compare against `a[write - 2]` rather than `a[write - 1]`, starting `write` at 2.' },
    { level: 'Medium', q: 'What is the space complexity of the two-pointer family?', a: '`O(1)` — that is much of the point. If a problem insists on constant extra space, two pointers or an in-place reversal is usually the intended route.' },
    { level: 'Medium', q: 'Sorted array, find pairs with a given difference.', a: 'Two pointers moving in the same direction: advance `hi` while the difference is too small, advance `lo` while it is too large, and record when equal. `O(n)` after sorting.' },
  ],
};

export const SLIDING_WINDOW_QUESTIONS: QuestionSet = {
  topic: 'sliding-window',
  questions: [
    { level: 'Easy', q: 'What phrases signal a sliding window?', a: '"Longest", "shortest", "at most K", "exactly K", "every window of size K", and above all "contiguous" or "consecutive". Without contiguity a window does not apply.' },
    { level: 'Medium', q: 'Why is a sliding window linear despite the nested `while`?', a: 'Both pointers only move forward and neither passes the end, so there are at most `n` additions and `n` removals across the entire run.' },
    { level: 'Medium', q: 'What is the difference between the longest and shortest templates?', a: 'Where you record the answer. For longest, record after restoring the invariant. For shortest, record while the window is still valid, before shrinking past it. Getting this backwards is the classic bug.' },
    { level: 'Medium', q: 'How do you handle "exactly K"?', a: '`atMost(K) - atMost(K-1)`. "Exactly" is not a monotone rule, so the window pointers cannot both stay monotonic; the subtraction sidesteps that.' },
    { level: 'Medium', q: 'Once a window is valid, how many subarrays end at `right`?', a: '`right - left + 1`. Any shorter window ending at `right` is also valid, since it can only have fewer distinct values or a smaller sum.' },
    { level: 'Medium', q: 'Maximum sum of `k` consecutive elements.', a: 'Build the first window, then slide: `sum += a[right] - a[right - k]`. The element leaving is at `right - k`, not `right - k + 1`.' },
    { level: 'Medium', q: 'Longest substring without repeating characters.', a: 'Extend right and count the character; while its count exceeds one, remove from the left. Only the entering character can break the invariant, so a single `while` suffices.' },
    { level: 'Hard', q: 'Minimum window substring — what state do you keep?', a: 'A map of required counts and a counter of how many requirements are currently satisfied. Expand until satisfied, then shrink while still satisfied, recording the best before each shrink.' },
    { level: 'Medium', q: 'Longest repeating character replacement with at most `k` changes.', a: 'Track the count of the most frequent character in the window. The window is valid while `length - maxCount <= k`; otherwise shrink from the left.' },
    { level: 'Medium', q: 'Fruit into baskets — what is it really?', a: 'Longest subarray with at most two distinct values. A window with a frequency map, shrinking while the map has more than two keys.' },
    { level: 'Medium', q: 'Why must you remove a key when its count hits zero?', a: 'Because the shrink condition usually tests `map.size()`. Leaving zero-count keys makes the map report more distinct values than the window contains.' },
    { level: 'Medium', q: 'Minimum size subarray with sum at least `S`, all positive.', a: 'Expand right adding to the sum; while the sum is at least `S`, record the length and shrink from the left. `O(n)`.' },
    { level: 'Hard', q: 'Why does that break if negatives are allowed?', a: 'Extending the window can decrease the sum, so an invalid window may become valid again later. The pointers stop being monotonic; use prefix sums with a monotonic deque instead.' },
    { level: 'Medium', q: 'Maximum in every window of size `k`.', a: 'A deque of indices with decreasing values: drop smaller values from the back before pushing, drop the front once it slides out of range, and the front is always the maximum. `O(n)`.' },
    { level: 'Hard', q: 'Why is it safe to discard smaller earlier values from the deque?', a: 'A smaller value that arrived earlier will leave the window no later than the larger newer one, and can never be the maximum while that larger value remains. It can never matter again.' },
    { level: 'Medium', q: 'Count anagrams of `p` inside `s`.', a: 'A fixed window of length `|p|` with character counts, compared against the target counts as the window slides. Comparing two 26-slot arrays is constant work.' },
    { level: 'Medium', q: 'Longest subarray with at most `k` zeroes flipped.', a: 'A window that shrinks while the count of zeroes inside exceeds `k`. The answer is the widest such window.' },
    { level: 'Medium', q: 'What is the first thing to write when solving a window problem?', a: 'The invariant, in one sentence. "The window contains no repeated character." The shrink condition and where to record the answer both follow from it.' },
    { level: 'Medium', q: 'Is a fixed-size window the same technique as a variable one?', a: 'The same idea with a simpler rule: add one, drop one, no `while`. Fixed size means the constraint is the length itself rather than a property of the contents.' },
    { level: 'Medium', q: 'How does a window differ from two pointers?', a: 'A window is the two-pointer, same-direction family where you also maintain state about everything between the pointers. Plain two pointers usually need no such state.' },
  ],
};

export const BINARY_SEARCH_QUESTIONS: QuestionSet = {
  topic: 'binary-search',
  questions: [
    { level: 'Easy', q: 'What does binary search actually require?', a: 'Monotonicity with respect to your question — the answer to "is the target left or right?" must be consistent everywhere. Sorted data is simply the most common way to get that.' },
    { level: 'Easy', q: 'Why is `mid = (lo + hi) / 2` a bug?', a: '`lo + hi` can overflow for large indices. Use `lo + (hi - lo) / 2`, which cannot.' },
    { level: 'Medium', q: 'Why does a binary search sometimes hang?', a: 'A branch that does not shrink the range — typically `lo = mid` when `mid` is already `lo`. Trace a two-element array by hand and the culprit appears at once.' },
    { level: 'Medium', q: 'Explain the half-open template.', a: 'Keep `[lo, hi)` with the answer always inside. Loop while `lo < hi`; if the condition holds at `mid`, set `hi = mid` (mid might be the answer), otherwise `lo = mid + 1`. When the loop ends, `lo` is the boundary.' },
    { level: 'Easy', q: 'What is the difference between lower bound and upper bound?', a: 'Lower bound is the first index with a value at least the target; upper bound is the first strictly greater. The count of the target is `upper - lower`.' },
    { level: 'Medium', q: 'How do you find the first and last occurrence of a value?', a: 'First is the lower bound (check it actually equals the target). Last is `upper bound - 1`.' },
    { level: 'Medium', q: 'Search in a rotated sorted array.', a: 'At each step at least one half is still sorted. Use `a[lo] <= a[mid]` to decide which, then check whether the target lies inside that sorted half and discard accordingly.' },
    { level: 'Hard', q: 'What breaks rotated search when duplicates are allowed?', a: '`a[lo] == a[mid] == a[hi]` tells you nothing about which half is sorted. The fallback is to shrink `lo` by one, which makes the worst case `O(n)` — say so rather than claiming the log bound.' },
    { level: 'Medium', q: 'Find the minimum in a rotated sorted array.', a: 'Compare `a[mid]` with `a[hi]`: if `a[mid] > a[hi]` the minimum is to the right, so `lo = mid + 1`; otherwise `hi = mid`. Comparing with `a[lo]` is the version that goes wrong.' },
    { level: 'Medium', q: 'How do you binary search a 2D matrix?', a: 'If rows are sorted and each row starts after the previous ends, treat it as one flat array of length `rows·cols`, mapping `mid` back with `/` and `%`.' },
    { level: 'Medium', q: 'A matrix sorted by rows and columns only — how do you search it?', a: 'Not with binary search. Start at the top-right corner: move left if the value is too large, down if too small. That is `O(rows + cols)`.' },
    { level: 'Medium', q: 'What phrase most reliably signals binary search on the answer?', a: '"Minimise the maximum" or "maximise the minimum". It almost always means a monotonic feasibility check exists.' },
    { level: 'Medium', q: 'Describe the shape of a binary-search-on-answer solution.', a: 'Identify what you are choosing, bound its range, write `feasible(x)` as a greedy or linear check, then binary search the first `x` where it becomes true.' },
    { level: 'Medium', q: 'Ship packages within `D` days — what do you search?', a: 'The ship capacity. Lower bound is the largest package, upper bound is the total weight, and `feasible(c)` counts the days needed at capacity `c`.' },
    { level: 'Medium', q: 'Koko eating bananas — what do you search?', a: 'The eating speed, from 1 to the largest pile. `feasible(s)` sums `ceil(pile / s)` and compares against the hours available.' },
    { level: 'Hard', q: 'Split an array into `m` parts minimising the largest sum.', a: 'Binary search the largest allowed sum between `max(a)` and `sum(a)`; `feasible(x)` greedily counts how many parts are needed if no part may exceed `x`.' },
    { level: 'Medium', q: 'Why must the feasibility check be monotonic?', a: 'Because binary search needs a single false-to-true boundary. If a larger capacity works, every capacity above it must also work — extra room never hurts.' },
    { level: 'Hard', q: 'Median of two sorted arrays in `O(log(min(n,m)))`.', a: 'Binary search the partition point in the shorter array. Choose how many elements come from each side so the halves are balanced, and check `maxLeft <= minRight` on both sides.' },
    { level: 'Medium', q: 'How do you binary search on real numbers?', a: 'Loop a fixed number of times — around 100 — or until `hi - lo` is below a tolerance. Exact equality never happens with floating point.' },
    { level: 'Medium', q: 'How do you search a stream or an API with unknown length?', a: 'Double an index until you overshoot, then binary search inside `[i/2, i]`. That is still `O(log n)` overall.' },
    { level: 'Medium', q: 'Explain peak finding.', a: 'Compare `a[mid]` with `a[mid+1]` and move toward the side going uphill. Nothing is sorted, yet each comparison still eliminates half — proof that binary search is about monotonic decisions, not sorted data.' },
    { level: 'Medium', q: 'Find a single element in a sorted array where every other appears twice.', a: 'Binary search on parity: before the unique element pairs start at even indices, after it they start at odd ones. Check whether `a[mid]` matches its partner to decide the side.' },
    { level: 'Medium', q: 'How would you find the square root of an integer without a library?', a: 'Binary search `x` in `[0, n]` for the largest value with `x·x <= n`, using 64-bit multiplication to avoid overflow.' },
    { level: 'Medium', q: 'What is the insertion point, and which function gives it?', a: 'The index where a value would go to keep the array sorted — that is exactly the lower bound.' },
    { level: 'Easy', q: 'Which library functions implement these?', a: '`Arrays.binarySearch` in Java (with adjustment for bounds), `lower_bound`/`upper_bound` in C++, `bisect_left`/`bisect_right` in Python.' },
    { level: 'Medium', q: 'What edge cases should a binary search always be tested on?', a: 'Empty array, one element, target absent, target smaller than everything, target larger than everything, and all elements equal.' },
    { level: 'Hard', q: 'Kth smallest element in a sorted matrix.', a: 'Binary search the value range, not the indices. For a candidate `x`, count how many entries are at most `x` with a staircase walk from the top-right, and adjust the range accordingly.' },
    { level: 'Medium', q: 'How do you know whether to search the index or the answer?', a: 'If the collection is sorted and you want a position, search the index. If the answer is a number in a known range with a monotone feasibility test, search the answer.' },
  ],
};
