import { QuestionSet } from './question.model';

export const SORTING_QUESTIONS: QuestionSet = {
  topic: 'sorting',
  questions: [
    { level: 'Easy', q: 'Compare merge sort and quick sort.', a: 'Merge sort is stable and always `O(n log n)` but needs `O(n)` extra memory. Quick sort is in place with `O(log n)` stack, usually faster in practice from cache locality, but has an `O(n²)` worst case and is not stable.' },
    { level: 'Easy', q: 'What does it mean for a sort to be stable?', a: 'Equal elements keep their original relative order. That is what lets you sort by a secondary key first and then by the primary key to get a correct two-level ordering.' },
    { level: 'Easy', q: 'Which standard sorts are stable?', a: 'Java sorts objects with a stable merge sort and primitives with an unstable dual-pivot quick sort. C++ `sort` is unstable, `stable_sort` is stable. Python `sorted` is always stable.' },
    { level: 'Medium', q: 'Why can no comparison sort beat `O(n log n)`?', a: 'A comparison sort is a decision tree; each comparison has two outcomes and every leaf is one of the `n!` orderings. A binary tree with `n!` leaves has height at least `log2(n!)`, which is `Omega(n log n)`.' },
    { level: 'Medium', q: 'How do counting and radix sort escape that bound?', a: 'They do not compare elements — they use the values themselves as indices. That requires assumptions about the value range, which the lower bound does not.' },
    { level: 'Medium', q: 'When is counting sort the wrong choice?', a: 'When the value range `k` is huge or unknown, because it allocates an array of size `k`. `k` appears in the complexity for a reason.' },
    { level: 'Medium', q: 'Explain quick sort partitioning.', a: 'Choose a pivot and rearrange so everything smaller sits left and everything larger sits right, then recurse on both sides. Lomuto uses a single write pointer; Hoare moves two pointers inward.' },
    { level: 'Medium', q: 'What causes quick sort\'s worst case, and how do you avoid it?', a: 'Consistently extreme pivots — for example always taking the last element on an already-sorted array. Use a random pivot or median-of-three, and say so when presenting the solution.' },
    { level: 'Medium', q: 'Why do library sorts switch to insertion sort for small subarrays?', a: 'Its constant factor beats the recursion and partitioning overhead below roughly sixteen elements, and it is very fast on nearly sorted input.' },
    { level: 'Medium', q: 'Explain quickselect.', a: 'Partition as in quick sort but recurse only into the side containing index `k`. Each step is `O(n)` and the remaining work halves on average, giving expected `O(n)`.' },
    { level: 'Medium', q: 'Kth largest element — which approach and when?', a: 'Quickselect for expected `O(n)` when the array is in memory. A min-heap of size `k` for `O(n log k)` when data streams in or `k` is small. Sorting only if you need the whole order.' },
    { level: 'Medium', q: 'How do you sort a linked list efficiently?', a: 'Merge sort. Splitting is `O(1)` once you find the middle, merging is a two-pointer walk, and no auxiliary array is needed — only `O(log n)` stack.' },
    { level: 'Medium', q: 'What is external sorting?', a: 'Sorting data too large for memory: sort chunks that fit, write them out, then k-way merge the runs with a heap. It is why merge sort still matters in practice.' },
    { level: 'Medium', q: 'Write a comparator that sorts by one field then another.', a: '`Comparator.comparingInt(x -> x[0]).thenComparingInt(x -> x[1])`. Avoid the `a - b` subtraction shortcut, which overflows for large values.' },
    { level: 'Medium', q: 'What makes a comparator invalid?', a: 'Inconsistency — it must be transitive and antisymmetric. Java throws "Comparison method violates its general contract"; C++ gives undefined behaviour.' },
    { level: 'Medium', q: 'Sort intervals — by start or by end?', a: 'By start to merge overlaps or to sweep. By end to select the maximum number of non-overlapping intervals. Choosing the key is the algorithm.' },
    { level: 'Medium', q: 'How do you sort strings by a custom alphabet order?', a: 'Map each character to its rank in the new alphabet, then compare rank arrays — or supply a comparator that compares by rank position by position.' },
    { level: 'Medium', q: 'How would you check whether an array can be sorted by one swap?', a: 'Compare against the sorted version and count mismatching positions. Zero means already sorted, exactly two means one swap suffices, anything else means no.' },
    { level: 'Medium', q: 'Merge `k` sorted arrays.', a: 'A min-heap holding one candidate per array, refilled after each extraction. `O(n log k)`, versus `O(n·k)` for scanning all heads each time.' },
    { level: 'Medium', q: 'Find the minimum number of swaps to sort an array.', a: 'Treat it as a permutation and count cycles: the answer is `n - numberOfCycles`, since a cycle of length `L` needs `L-1` swaps.' },
    { level: 'Hard', q: 'Count inversions in an array.', a: 'Modify merge sort: when taking an element from the right half, every remaining element in the left half forms an inversion with it. `O(n log n)`. A Fenwick tree also works.' },
    { level: 'Medium', q: 'What does "sorting as preprocessing" mean?', a: 'You rarely sort because the output must be ordered — you sort so that binary search, two pointers, greedy scans and duplicate handling all become available afterwards.' },
    { level: 'Medium', q: 'Sort colours / Dutch national flag — why one pass?', a: 'Three pointers maintain three regions and each element is examined once. Two counting passes also work but touch the data twice.' },
    { level: 'Easy', q: 'What is the best case for insertion sort and why?', a: '`O(n)` on already sorted input: the inner shift loop never runs, so it is a single pass of comparisons.' },
    { level: 'Easy', q: 'What is heap sort\'s advantage?', a: 'Guaranteed `O(n log n)` with `O(1)` extra space. Its disadvantages are instability and worse cache behaviour than quick sort.' },
    { level: 'Medium', q: 'Is `Arrays.sort` on primitives stable in Java?', a: 'No — it uses dual-pivot quick sort. Stability only matters for objects with distinguishable equal keys, which is why primitives can use the faster unstable sort.' },
    { level: 'Medium', q: 'How do you find the largest number formed by concatenating an array?', a: 'Sort with the comparator `(a, b) -> (b + a).compareTo(a + b)`, comparing the two concatenations rather than the numbers. Handle the all-zeroes case.' },
    { level: 'Medium', q: 'Meeting rooms — how much sorting do you need?', a: 'Sort by start, then a min-heap of end times gives the peak concurrency. Alternatively sort start and end times separately and sweep both with two pointers.' },
    { level: 'Medium', q: 'What is bucket sort and when does it work?', a: 'Distribute values into buckets by range, sort each bucket, then concatenate. It is `O(n)` expected when values are roughly uniform, and degenerates when they cluster.' },
    { level: 'Medium', q: 'How would you sort a nearly sorted array where each element is at most `k` away from its place?', a: 'A min-heap of size `k+1`: push as you scan and pop the minimum each step. `O(n log k)`, much better than a general sort.' },
  ],
};

export const RECURSION_QUESTIONS: QuestionSet = {
  topic: 'recursion',
  questions: [
    { level: 'Easy', q: 'How do you write a recursive function without getting lost?', a: 'State the contract in one sentence, write the base case, assume the function already works for smaller inputs, and combine what it returns. Do not trace the calls in your head.' },
    { level: 'Easy', q: 'What are the two mandatory parts of any recursion?', a: 'A base case that returns without recursing, and a recursive case that moves strictly closer to it.' },
    { level: 'Medium', q: 'How do you find the complexity of a recursive function?', a: 'Draw the recursion tree. Multiply the number of nodes by the work done per node outside the recursive calls.' },
    { level: 'Medium', q: 'Why is naive Fibonacci exponential?', a: 'Each call spawns two more with almost the full remaining depth, so the tree roughly doubles per level — `O(2ⁿ)` — and the same subproblems are recomputed many times.' },
    { level: 'Medium', q: 'What is the space complexity of recursion with no allocations?', a: '`O(depth)` for the stack frames. Claiming constant space for a recursive DFS on a skewed tree is a common and costly slip.' },
    { level: 'Medium', q: 'Explain the backtracking template.', a: 'Choose, explore, un-choose: apply a candidate, recurse, then undo it so the next branch starts clean. Prune candidates that cannot lead to a solution before recursing.' },
    { level: 'Medium', q: 'How do you generate all subsets?', a: 'At each index, recurse without the element and then with it, undoing the addition afterwards. That is `2ⁿ` results with `O(n)` depth. For `n <= 20` a bitmask loop does the same iteratively.' },
    { level: 'Medium', q: 'How do you generate all permutations?', a: 'At each position, try every unused element: mark used, add, recurse, then unmark and remove. `O(n!)` results.' },
    { level: 'Medium', q: 'Why must you copy the working list when recording a result?', a: 'You stored a reference to a list that keeps mutating, so every recorded result ends up identical. Add a copy instead.' },
    { level: 'Medium', q: 'How do you avoid duplicate subsets when the input has repeats?', a: 'Sort first, then at each depth skip a candidate equal to the previous one — `if (i > start && a[i] == a[i-1]) continue`. `i > start` scopes the skip to the current level.' },
    { level: 'Medium', q: 'Why `i > start` and not `i > 0`?', a: 'Because the skip must only suppress duplicates at the same depth, not legitimate reuse deeper in the tree. `start` is the first candidate considered at this level.' },
    { level: 'Medium', q: 'Combination sum where numbers can be reused — what changes?', a: 'Recurse with `i` instead of `i + 1`, so the same candidate can be chosen again. Sorting lets you `break` once a candidate exceeds the remaining target.' },
    { level: 'Medium', q: 'N-Queens — what state do you track?', a: 'One queen per row, so only the column matters. Track used columns and both diagonals — `row + col` and `row - col` — as sets, which makes each validity check `O(1)`.' },
    { level: 'Medium', q: 'Word search in a grid — what makes it correct?', a: 'Mark the cell as visited before recursing and restore it afterwards, so other branches can use it. Forgetting the restore silently loses valid paths.' },
    { level: 'Medium', q: 'What kinds of pruning matter most?', a: 'Constraint pruning (abandon an already invalid branch), bound pruning (the best possible completion is still worse than a known answer), sorting so you can `break`, and duplicate pruning.' },
    { level: 'Medium', q: 'When should you convert recursion to iteration?', a: 'When depth can reach hundreds of thousands, when the recursion is a plain linear chain (which is just a loop), or when you need explicit control over traversal order.' },
    { level: 'Medium', q: 'How do you convert a recursive DFS to an iterative one?', a: 'Use an explicit stack holding whatever the frame held. For a preorder tree walk, push right before left so the left subtree pops first.' },
    { level: 'Medium', q: 'What is tail recursion, and does it help in Java?', a: 'A recursive call that is the last operation, so the frame could be reused. Java and Python do not eliminate tail calls, so it does not help there — rewrite as a loop.' },
    { level: 'Medium', q: 'Generate all balanced parenthesis strings of length `2n`.', a: 'Track open and close counts. You may add "(" while open is below `n`, and ")" while close is below open. That invariant prunes every invalid branch before it starts.' },
    { level: 'Medium', q: 'Solve Sudoku — what makes it feasible?', a: 'Constraint propagation: keep row, column and box sets so validity is `O(1)`, and pick the most constrained empty cell first. Without pruning the raw search never finishes.' },
    { level: 'Medium', q: 'How is recursion related to dynamic programming?', a: 'DP is recursion whose repeated subproblems are cached. If the recursion tree shows the same call more than once, memoising collapses it from exponential to polynomial.' },
    { level: 'Medium', q: 'Tower of Hanoi — what is the recurrence?', a: '`T(n) = 2T(n-1) + 1`, giving `2ⁿ - 1` moves: shift `n-1` discs aside, move the largest, then shift the `n-1` back.' },
    { level: 'Medium', q: 'How do you print all paths from the root to every leaf?', a: 'Carry the current path down, append at each node, recurse into the children, and remove on the way out. Record a copy of the path at each leaf.' },
    { level: 'Easy', q: 'What does "trust the recursive contract" mean in practice?', a: 'Assume the call on a smaller input returns the right answer, and focus only on combining those answers. If the contract is precise, the body is usually three lines.' },
    { level: 'Medium', q: 'Why is a subset enumeration `2ⁿ` and a permutation enumeration `n!`?', a: 'Subsets make one binary in-or-out decision per element. Permutations choose which of the remaining elements comes next, giving `n · (n-1) · … · 1`.' },
  ],
};

export const LINKED_LIST_QUESTIONS: QuestionSet = {
  topic: 'linked-lists',
  questions: [
    { level: 'Easy', q: 'When is a linked list better than an array?', a: 'When you already hold a node and need `O(1)` insertion or removal there, or when you insert at the front constantly. Arrays win almost everywhere else because of indexing and cache locality.' },
    { level: 'Easy', q: 'What is the golden rule of pointer manipulation?', a: 'Save the next node before overwriting a link. Reassigning `node.next` without keeping the tail makes the rest of the list unreachable.' },
    { level: 'Medium', q: 'Reverse a linked list iteratively.', a: 'Three pointers: `previous`, `current` and a saved `ahead`. Each step saves `current.next`, points `current.next` at `previous`, then advances both. Return `previous`. `O(n)` time, `O(1)` space.' },
    { level: 'Medium', q: 'Reverse it recursively — what is the trade-off?', a: 'Recurse to the end, then `head.next.next = head` and `head.next = null` on the way back. Elegant, but `O(n)` stack, so it overflows on a very long list.' },
    { level: 'Medium', q: 'Find the middle of a list in one pass.', a: 'Slow moves one, fast moves two; when fast runs out, slow is at the middle. For an even length decide up front whether you want the first or second middle.' },
    { level: 'Medium', q: 'Find the nth node from the end.', a: 'Advance a lead pointer `n` steps, then move both until the lead reaches the end. The trailing pointer is the answer — one pass, no length computation.' },
    { level: 'Medium', q: 'Detect a cycle in a linked list.', a: "Floyd's algorithm: slow moves one, fast moves two. If they ever meet there is a cycle; if fast reaches null there is not. `O(n)` time, `O(1)` space." },
    { level: 'Hard', q: 'Find the node where the cycle begins.', a: 'After the meeting, reset one pointer to the head and advance both one step at a time. They meet at the entry, because the distance from head to entry equals the distance from the meeting point to the entry.' },
    { level: 'Medium', q: 'Why is a dummy head useful?', a: 'It removes every special case at the front. Deletions, insertions and merges all become the general case, and you return `dummy.next` at the end.' },
    { level: 'Medium', q: 'Merge two sorted linked lists.', a: 'A dummy head and a tail pointer: repeatedly attach the smaller head and advance, then attach whatever remains. `O(n + m)`, `O(1)` extra.' },
    { level: 'Medium', q: 'Merge `k` sorted lists.', a: 'A min-heap of the current heads, `O(n log k)`. Or merge pairwise in rounds, which is the same complexity with no heap.' },
    { level: 'Medium', q: 'Remove the nth node from the end.', a: 'Dummy head, lead pointer `n+1` steps ahead, then advance both. When the lead hits null the trailing pointer sits just before the target, so unlink it.' },
    { level: 'Medium', q: 'Detect the intersection of two lists.', a: 'Walk both, switching to the other list on reaching the end. They travel `lenA + lenB` and meet at the intersection, or both reach null together. `O(1)` space.' },
    { level: 'Medium', q: 'Check whether a list is a palindrome in `O(1)` space.', a: 'Find the middle, reverse the second half, compare the halves, then restore the list. Interviewers often ask for the restore.' },
    { level: 'Medium', q: 'Why is merge sort the right sort for a linked list?', a: 'Splitting is `O(1)` once you have the middle and merging needs no auxiliary array, so it is `O(n log n)` time with only stack space. Quick sort needs random access it does not have.' },
    { level: 'Medium', q: 'What goes wrong splitting a two-node list for merge sort?', a: 'If both pointers start at the head, `slow` lands on the second node and the split produces an empty half — infinite recursion. Start `fast` at `head.next`.' },
    { level: 'Medium', q: 'Remove duplicates from a sorted list.', a: 'Walk with one pointer, skipping nodes whose value equals the current one. To remove **all** copies of duplicated values, use a dummy head and compare `previous.next` against the run.' },
    { level: 'Medium', q: 'Reorder a list as first, last, second, second-last…', a: 'Find the middle, reverse the second half, then interleave the two halves. All in `O(1)` space.' },
    { level: 'Hard', q: 'Reverse nodes in groups of `k`.', a: 'Check that `k` nodes remain, reverse that block iteratively, connect it to the previous block, and recurse or loop on the rest. Leave a trailing partial group untouched.' },
    { level: 'Hard', q: 'Design an LRU cache.', a: 'A hash map from key to node, plus a doubly linked list ordered by recency. `get` unlinks the node and moves it to the front; `put` inserts at the front and evicts the tail when over capacity. All `O(1)`.' },
    { level: 'Hard', q: 'Why must the LRU list be doubly linked?', a: 'Unlinking a node in `O(1)` requires its predecessor. In a singly linked list you would have to walk from the head, making the operation `O(n)`.' },
    { level: 'Medium', q: 'Copy a list with random pointers.', a: 'Interleave copies into the original list so each copy sits beside its source, set the random pointers using that adjacency, then separate the two lists. `O(1)` extra space beyond the output.' },
    { level: 'Medium', q: 'Add two numbers represented as linked lists.', a: 'If digits are stored least-significant-first, walk both with a carry. If most-significant-first, reverse both first or use two stacks.' },
    { level: 'Medium', q: 'Flatten a multilevel doubly linked list.', a: 'Depth-first: when a node has a child, splice the child list in between the node and its next, fix all the previous pointers, and continue.' },
    { level: 'Medium', q: 'Rotate a list right by `k`.', a: 'Find the length, close it into a ring, then break it `length - k % length` nodes along. Take `k %= length` first.' },
    { level: 'Easy', q: 'Why write `fast != null && fast.next != null` in that order?', a: 'Short-circuit evaluation. If `fast` is null the second test never runs; reversing the order dereferences null on the first even-length list you try.' },
    { level: 'Medium', q: 'How would you detect a cycle without the two-pointer trick?', a: 'A hash set of visited nodes — `O(n)` time but `O(n)` space. The two-pointer version exists precisely to get the space down to constant.' },
    { level: 'Medium', q: 'Partition a list around a value `x`.', a: 'Build two lists with dummy heads — one for values below `x`, one for the rest — then join them. Stable, one pass, `O(1)` extra.' },
    { level: 'Medium', q: 'Swap nodes in pairs.', a: 'A dummy head and careful relinking of three pointers per pair, or recursion where each call swaps the first two and attaches the result of the rest.' },
    { level: 'Easy', q: 'What edge cases does every linked-list answer need?', a: 'Empty list, one node, two nodes, and the operation targeting the head or the tail. Most bugs live in exactly those four cases.' },
  ],
};
