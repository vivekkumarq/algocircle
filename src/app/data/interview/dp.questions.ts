import { QuestionSet } from './question.model';

export const DP_QUESTIONS: QuestionSet = {
  topic: 'dynamic-programming',
  questions: [
    { level: 'Easy', q: 'When does dynamic programming apply?', a: 'When subproblems overlap — the same smaller problem appears more than once — and there is optimal substructure, so an optimal answer is built from optimal answers to those subproblems.' },
    { level: 'Easy', q: 'What is the difference between memoisation and tabulation?', a: 'Memoisation is the recursion with a cache: only reachable states are computed, but it costs stack depth. Tabulation fills a table in dependency order with no recursion, and makes space optimisation easier.' },
    { level: 'Easy', q: 'What are the three steps of designing a DP?', a: 'Define the state in one sentence, write the transition as the list of choices available, then fix the base cases and the iteration order.' },
    { level: 'Medium', q: 'My DP gives wrong answers. What is usually the cause?', a: 'The state carries too little information. If two different situations map to the same state but have different answers, add the dimension that separates them.' },
    { level: 'Medium', q: 'How do you compute the complexity of a DP?', a: 'Number of states multiplied by the work per transition. `dp[i][c]` with an `O(1)` transition is `O(n·c)`; with an inner loop over `k` choices it is `O(n·c·k)`.' },
    { level: 'Easy', q: 'Climbing stairs — state and transition.', a: '`dp[i]` is the number of ways to reach step `i`; `dp[i] = dp[i-1] + dp[i-2]`. Since it looks back only two steps, two variables suffice.' },
    { level: 'Medium', q: 'House robber — what is the choice?', a: 'At each house, either rob it and skip the previous, or skip it and keep the best so far: `take = skip + value`, `skip = max(skip, take)`. `O(n)` time, `O(1)` space.' },
    { level: 'Medium', q: 'House robber in a circle — what changes?', a: 'The first and last houses are adjacent, so run the linear version twice — once excluding the first house, once excluding the last — and take the maximum.' },
    { level: 'Medium', q: 'Unique paths in a grid.', a: '`dp[r][c] = dp[r-1][c] + dp[r][c-1]`, with the first row and column set to 1. Reduces to a single row because only the previous row is read.' },
    { level: 'Medium', q: 'Minimum path sum — how does it differ?', a: 'Same grid shape with `min` instead of a sum, adding the current cell value. Obstacles are handled by setting those cells to zero ways or infinite cost.' },
    { level: 'Medium', q: 'Explain 0/1 knapsack.', a: '`dp[i][c]` is the best value from the first `i` items with capacity `c`. For each item take the better of skipping it or taking it: `max(dp[i-1][c], value[i] + dp[i-1][c - w[i]])`.' },
    { level: 'Medium', q: 'How do you reduce knapsack to one row?', a: 'Iterate the capacity **backwards**. That guarantees `dp[c - w]` still refers to the previous item, so each item is used at most once.' },
    { level: 'Medium', q: 'What happens if you iterate the capacity forwards?', a: 'You get the unbounded knapsack, where each item may be reused — because `dp[c - w]` now already includes the current item. One character changes the problem being solved.' },
    { level: 'Medium', q: 'Coin change — minimum coins.', a: 'Unbounded knapsack taking a minimum: `dp[amount] = min(dp[amount], dp[amount - coin] + 1)`. Initialise with a large sentinel and check for it at the end to report "impossible".' },
    { level: 'Medium', q: 'Coin change — number of ways. What is the ordering trap?', a: 'Loop coins on the **outside** and amounts inside. The other order counts permutations as distinct, so `1+2` and `2+1` are both counted.' },
    { level: 'Medium', q: 'Subset sum and equal partition.', a: 'Subset sum is a boolean knapsack. Equal partition is subset sum for `total / 2`, which is impossible immediately if the total is odd.' },
    { level: 'Medium', q: 'Target sum with plus and minus signs.', a: 'Assigning signs is equivalent to choosing a positive subset `P` with `sum(P) = (total + target) / 2`. Check that this is a non-negative even value, then it is plain subset sum.' },
    { level: 'Medium', q: 'Longest common subsequence — state and transition.', a: '`dp[i][j]` is the LCS of the first `i` and first `j` characters. If the characters match, `dp[i-1][j-1] + 1`; otherwise the maximum of dropping one from either side.' },
    { level: 'Medium', q: 'Longest common substring — how does it differ from LCS?', a: 'It must be contiguous, so on a mismatch the cell resets to zero rather than inheriting a neighbour. The answer is the maximum cell, not the corner.' },
    { level: 'Medium', q: 'Edit distance — what are the three predecessors?', a: 'Replace comes from the diagonal, delete from above, insert from the left; add one unless the characters match, in which case take the diagonal unchanged.' },
    { level: 'Medium', q: 'Longest palindromic subsequence in one line of insight.', a: 'It is the LCS of the string and its reverse. The minimum insertions to make a palindrome is `n` minus that.' },
    { level: 'Medium', q: 'Longest increasing subsequence — both solutions.', a: '`O(n²)`: `dp[i]` is the best subsequence ending at `i`, extended from any earlier smaller value. `O(n log n)`: keep the smallest possible tail for each length and binary search where each value belongs.' },
    { level: 'Medium', q: 'In the `O(n log n)` LIS, is the `tails` array a valid subsequence?', a: 'No — only its length is meaningful. Reconstructing the actual subsequence needs a separate array of predecessor indices.' },
    { level: 'Medium', q: 'Word break — state and transition.', a: '`dp[i]` is whether the first `i` characters can be segmented. It is true if some `j < i` has `dp[j]` true and `s[j..i)` in the dictionary. `O(n²)` with a hash set.' },
    { level: 'Medium', q: 'Decode ways — what are the traps?', a: 'Zero cannot start a code, "10" and "20" have exactly one decoding, and anything above 26 cannot be taken as a pair. Most wrong answers mishandle zeroes.' },
    { level: 'Medium', q: 'What is interval DP and how do you iterate it?', a: '`dp[i][j]` over a range, trying every split point inside. Iterate by increasing length so shorter intervals are ready — matrix chain multiplication, burst balloons, palindrome partitioning.' },
    { level: 'Hard', q: 'Burst balloons — why think backwards?', a: 'Forward, the neighbours change as you burst. Instead ask which balloon is burst **last** in a range: its neighbours are then the fixed range boundaries, and the two sides become independent subproblems.' },
    { level: 'Medium', q: 'What is bitmask DP and when is it usable?', a: 'The state includes a subset encoded as an integer, so it is `O(2ⁿ · n)`. Feasible for `n` up to about 20 — assignment problems and travelling salesman.' },
    { level: 'Medium', q: 'What is tree DP?', a: 'A DP where the state lives on a tree node, usually `dp[node][state]`. Each node combines the answers of its children — house robber on a tree, maximum independent set.' },
    { level: 'Medium', q: 'Explain state-machine DP with the stock problems.', a: 'The state is whether you currently hold a share, plus any transaction or cooldown counter. Each day you either act or do not, so the transitions are two lines: `cash = max(cash, hold + price)` and `hold = max(hold, cash - price)`.' },
    { level: 'Hard', q: 'What is digit DP?', a: 'Counting numbers up to `N` with some digit property. The state is position, a "tight" flag for whether the prefix matches `N`, and whatever property you are tracking.' },
    { level: 'Medium', q: 'How do you decide between DP and greedy?', a: 'If a locally best choice can ever block a better overall answer, you need DP. If you can prove an exchange argument, greedy is safe and much shorter.' },
    { level: 'Medium', q: 'How do you reduce DP space?', a: 'Ask how far back the transition reads. Only the previous row means one row; only two cells means two variables. Iterate in the direction that does not overwrite values still needed.' },
    { level: 'Medium', q: 'What do you lose by optimising the space?', a: 'The ability to reconstruct the answer. If the problem wants the actual subsequence or path rather than its length, keep the full table or store predecessors.' },
    { level: 'Medium', q: 'How do you reconstruct the solution from a DP table?', a: 'Walk backwards from the final cell, at each step choosing the predecessor that produced its value. For LCS, move diagonally on a match and toward the larger neighbour otherwise.' },
    { level: 'Medium', q: 'Partition equal subset sum — what is the complexity?', a: '`O(n · sum)` time and `O(sum)` space after reduction. It is pseudo-polynomial: the cost depends on the magnitude of the numbers, not just how many there are.' },
    { level: 'Medium', q: 'What does pseudo-polynomial mean?', a: 'Polynomial in the numeric value of the input rather than in its encoded size. Knapsack is `O(n·W)`, which is fine for small `W` but exponential in the number of bits of `W`.' },
    { level: 'Medium', q: 'Matrix chain multiplication — what is the state?', a: '`dp[i][j]` is the minimum cost of multiplying matrices `i` through `j`, minimising over every split point `k`. `O(n³)` with `O(n²)` space.' },
    { level: 'Medium', q: 'How do you approach a DP problem you cannot see the state for?', a: 'Write the brute-force recursion first, ignoring efficiency. Then look at which arguments actually vary — those are the state, and the cache follows immediately.' },
    { level: 'Medium', q: 'How should you present a DP solution in an interview?', a: 'Say the state out loud before writing code: "`dp[i][j]` is the edit distance between the first `i` characters of A and the first `j` of B." Interviewers are assessing whether you can define the state; the loops are mechanical.' },
  ],
};

export const ADVANCED_QUESTIONS: QuestionSet = {
  topic: 'advanced',
  questions: [
    { level: 'Easy', q: 'What triggers reaching for an advanced structure?', a: 'The same kind of query — over a range or a prefix — asked thousands of times, especially against data that keeps changing. One linear scan per query is what these structures remove.' },
    { level: 'Medium', q: 'When is a prefix-sum array enough, and when is it not?', a: 'Enough when the data never changes: `O(1)` queries after `O(n)` preprocessing. The moment a single value can be updated it collapses, because the whole suffix must be rebuilt.' },
    { level: 'Medium', q: 'Fenwick tree or segment tree?', a: 'Fenwick for prefix sums with point updates — ten lines and a tiny constant. Segment tree for anything else: min, max, gcd, custom merges, or range updates with lazy propagation.' },
    { level: 'Medium', q: 'What does `i & -i` do in a Fenwick tree?', a: 'Isolates the lowest set bit, which is the size of the block that index owns. Adding it jumps to the next covering block; subtracting it walks to the previous disjoint one. Both loops run once per set bit.' },
    { level: 'Medium', q: 'What is the classic Fenwick tree bug?', a: 'Index confusion. The structure is 1-indexed by construction, so convert 0-indexed input at the boundary and never in the middle.' },
    { level: 'Medium', q: 'How does a segment tree answer a range query?', a: 'It stores an aggregate for every range in a binary hierarchy. Any query decomposes into `O(log n)` fully covered nodes: return the identity for no overlap, the stored value for full cover, and recurse on partial.' },
    { level: 'Hard', q: 'Explain lazy propagation.', a: 'A range update stops at the highest nodes fully covering the range, storing the pending change there. Any later operation that needs to look inside pushes the pending value down first.' },
    { level: 'Hard', q: 'What invariant does lazy propagation maintain?', a: "A node's own aggregate is always correct, but its children may not yet reflect a pending update. Every operation that descends must push first, or answers become subtly stale rather than obviously broken." },
    { level: 'Medium', q: 'What is a sparse table good for?', a: 'Immutable range minimum, maximum or gcd in `O(1)` per query after `O(n log n)` preprocessing. It cannot do sums, because the two overlapping blocks would double-count.' },
    { level: 'Medium', q: 'Why does a sparse table need an idempotent operation?', a: 'The query covers the range with two power-of-two blocks that overlap. Overlap is harmless for min, max and gcd, but not for anything that accumulates.' },
    { level: 'Medium', q: 'What is a trie and when does it beat a hash set?', a: 'A tree indexed by prefix, one node per prefix. It wins for prefix queries and autocomplete, gives sorted order for free, and shares memory across common prefixes.' },
    { level: 'Medium', q: 'When is a trie the wrong choice?', a: 'For exact membership only, or with a large alphabet where 26 or more pointers per node is expensive, or when words share little so the structure degenerates.' },
    { level: 'Hard', q: 'How does a binary trie solve maximum XOR?', a: 'Insert numbers bit by bit, most significant first. To maximise the XOR with a query, greedily walk to the opposite branch at each bit when it exists. That turns an `O(n²)` pairwise search into `O(n · 32)`.' },
    { level: 'Medium', q: 'What is Aho-Corasick?', a: 'A trie of all patterns with KMP-style failure links, so a single pass over the text finds every pattern. `O(total pattern length + text length)`, instead of one pass per pattern.' },
    { level: 'Medium', q: 'What does a suffix array give you?', a: 'The sorted order of all suffixes. With the LCP array it answers longest repeated substring, number of distinct substrings, and longest common substring of two strings.' },
    { level: 'Medium', q: 'What is a Bloom filter and what does it promise?', a: 'A bit array with `k` hash functions. It can say "definitely not present" or "probably present" — false positives happen, false negatives never do. That asymmetry makes it a cheap pre-check before an expensive lookup.' },
    { level: 'Medium', q: 'What is a skip list?', a: 'Layered linked lists where each level skips more elements, with levels chosen randomly. It gives expected `O(log n)` search and is much simpler to implement than a balanced tree — Redis uses it for sorted sets.' },
    { level: 'Medium', q: 'Which balanced tree do standard libraries use, and why?', a: 'Red-black trees. They balance less strictly than AVL, so they rotate less on updates, which suits general-purpose maps. AVL is preferred when lookups dominate.' },
    { level: 'Medium', q: 'Why do databases use B-trees rather than binary trees?', a: 'Nodes are sized to a disk page, so one read fetches many keys. The tree is far shallower, which minimises disk reads — the operation that actually costs anything.' },
    { level: 'Medium', q: 'What is square-root decomposition?', a: 'Split the array into blocks of about `sqrt(n)` and precompute an aggregate per block. Queries touch `O(sqrt n)` blocks. It is simpler than a segment tree and often fast enough.' },
    { level: 'Medium', q: 'How do you count inversions?', a: 'Merge sort counting how many left-half elements remain when a right-half element is taken, or a Fenwick tree over compressed values scanning from the right. Both are `O(n log n)`.' },
    { level: 'Medium', q: 'What is coordinate compression and why is it needed?', a: 'Mapping large or sparse values onto `0..n-1` by sorting the distinct values. It lets Fenwick and segment trees be sized by the number of values rather than their magnitude.' },
    { level: 'Medium', q: 'How would you answer "sum of a range" with frequent updates?', a: 'A Fenwick tree — `O(log n)` for both. A prefix array would be `O(n)` per update and a plain scan `O(n)` per query.' },
    { level: 'Medium', q: 'What is the guiding rule when choosing among these?', a: 'Pick the weakest structure that answers the query. A prefix-sum array beats a segment tree whenever the data is static, and reaching for the heavier tool without need counts against you.' },
  ],
};
