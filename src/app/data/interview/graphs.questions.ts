import { QuestionSet } from './question.model';

export const GRAPHS_QUESTIONS: QuestionSet = {
  topic: 'graphs',
  questions: [
    { level: 'Easy', q: 'What is the first question to ask about a graph problem?', a: 'What is a vertex and what is an edge? Most graph problems never mention graphs — a word with one letter changed, a puzzle state, a course and its prerequisite, a grid cell and its neighbour.' },
    { level: 'Easy', q: 'Adjacency list or adjacency matrix?', a: 'List almost always: `O(V + E)` space and neighbour iteration proportional to degree. Matrix only for dense graphs or when you need `O(1)` edge existence tests, as in Floyd-Warshall.' },
    { level: 'Easy', q: 'Should you build an adjacency list for a grid?', a: 'No. A grid is an implicit graph — compute neighbours on the fly with a direction array. Building the list wastes memory and code.' },
    { level: 'Easy', q: 'When does BFS give a shortest path?', a: 'When every edge costs the same. BFS settles vertices in order of edge count, so the first time it reaches a vertex it has used the fewest possible edges.' },
    { level: 'Medium', q: 'Why mark a vertex visited when you enqueue it, not when you dequeue it?', a: 'Marking on dequeue lets the same vertex be enqueued many times before it is processed, blowing up the queue and producing wrong distances.' },
    { level: 'Medium', q: 'What is multi-source BFS?', a: 'Seed the queue with every source at distance zero. It answers "distance to the nearest of these" in one pass — rotting oranges, nearest gate, walls and gates.' },
    { level: 'Medium', q: 'What is 0-1 BFS?', a: 'For edges costing only 0 or 1: use a deque, pushing zero-weight moves to the front and one-weight moves to the back. That gives Dijkstra behaviour in `O(V + E)` with no heap.' },
    { level: 'Medium', q: 'When do you choose DFS over BFS?', a: 'For structure: cycle detection, topological order, connected components, bridges and articulation points, and any problem about paths rather than distances.' },
    { level: 'Medium', q: 'How do you detect a cycle in an undirected graph?', a: 'DFS tracking the parent: an already-visited neighbour that is not the parent closes a cycle. Union-find also works — an edge whose endpoints are already connected creates one.' },
    { level: 'Medium', q: 'How do you detect a cycle in a directed graph?', a: 'Three colours: unvisited, in-progress, done. An edge to an in-progress vertex is a back edge and proves a cycle. Kahn\'s algorithm detects it too, by producing fewer than `V` vertices.' },
    { level: 'Medium', q: 'Why is the parent check not enough for directed graphs?', a: 'You can reach an already-finished vertex without any cycle, because two paths can converge. Only an edge back to a vertex still on the current recursion path proves a cycle.' },
    { level: 'Medium', q: 'What is a topological order and when does it exist?', a: 'An ordering of vertices where every edge points forward. It exists exactly when the graph is a DAG — no cycles.' },
    { level: 'Medium', q: "Explain Kahn's algorithm.", a: 'Compute in-degrees, queue every vertex with in-degree zero, and repeatedly take one, output it, and decrement its neighbours. If fewer than `V` vertices come out, a cycle exists.' },
    { level: 'Medium', q: 'How do you get a topological order with DFS?', a: 'Run DFS and prepend each vertex to the output when it finishes. The result is a valid topological order, though detecting a cycle needs the three-colour state.' },
    { level: 'Medium', q: 'Course schedule — which algorithm and why?', a: 'Topological sort. "Can all courses be finished" is "is this a DAG"; "give an order" is the topological order itself.' },
    { level: 'Medium', q: 'How do you check whether a graph is bipartite?', a: 'Two-colour it with BFS or DFS; a conflicting edge means an odd cycle, so it is not bipartite. Loop over all vertices, because the graph may be disconnected.' },
    { level: 'Medium', q: 'Count connected components.', a: 'Loop over every vertex and start a traversal whenever you meet an unvisited one; each traversal consumes exactly one component. Union-find is the alternative.' },
    { level: 'Medium', q: 'Number of islands — describe the solution.', a: 'For each unvisited land cell, run DFS or BFS over its four neighbours, sinking the region as you go, and count how many times you started. `O(rows × cols)`.' },
    { level: 'Medium', q: 'Explain Dijkstra.', a: 'A priority queue always settles the nearest unfinished vertex. Relax its edges, push improved distances, and skip stale heap entries. `O((V + E) log V)`.' },
    { level: 'Medium', q: 'Why does Dijkstra fail with negative edges?', a: 'It finalises a vertex the first time it is popped, assuming no later path can be shorter. A negative edge can make a longer-looking route cheaper afterwards, invalidating that assumption.' },
    { level: 'Medium', q: 'What is a stale heap entry and how do you handle it?', a: 'An old, larger distance still sitting in the queue after a better one was found. Skip it with `if (d > dist[node]) continue;` — that is cheaper than trying to decrease keys.' },
    { level: 'Medium', q: 'Explain Bellman-Ford.', a: 'Relax every edge `V-1` times, because a shortest path has at most `V-1` edges. One more improving pass proves a negative cycle. `O(V·E)`.' },
    { level: 'Medium', q: 'Explain Floyd-Warshall, and its classic trap.', a: 'Try each vertex `k` as an intermediate stop for every pair. `k` **must** be the outermost loop; any other order silently computes something that is not the shortest path. `O(V³)`.' },
    { level: 'Medium', q: 'Shortest path in a DAG with arbitrary weights?', a: 'Relax edges in topological order — `O(V + E)`, and it handles negative weights, which Dijkstra cannot.' },
    { level: 'Medium', q: 'What does disjoint set union give you?', a: 'Near-constant "are these connected" and "merge these" operations, with path compression and union by size. It is the engine inside Kruskal and behind most connectivity questions.' },
    { level: 'Medium', q: 'What do path compression and union by size each do?', a: 'Path compression flattens the tree during `find`; union by size keeps the shallower tree under the deeper one. Together they give an amortised inverse-Ackermann cost, effectively constant.' },
    { level: 'Medium', q: 'Compare Kruskal and Prim.', a: 'Kruskal sorts edges and adds one whenever it joins two different components — better for sparse graphs with an edge list. Prim grows one tree using a heap of crossing edges — better for dense graphs with adjacency lists.' },
    { level: 'Hard', q: 'Why is the greedy MST choice correct?', a: 'The cut property: for any split of the vertices, the cheapest edge crossing it belongs to some MST. Both algorithms only ever add such an edge, which makes a local choice globally optimal.' },
    { level: 'Medium', q: 'How many edges does a spanning tree have?', a: 'Exactly `V - 1`. If your MST loop ends with fewer, the graph was disconnected.' },
    { level: 'Medium', q: 'Word ladder — what is the graph?', a: 'Each word is a vertex; an edge joins words differing in one letter. All edges cost the same, so BFS gives the shortest transformation. Precompute wildcard buckets to avoid comparing every pair.' },
    { level: 'Medium', q: 'Clone a graph — what do you need?', a: 'A map from original node to its copy, filled as you traverse. That map is what stops you looping forever on cycles.' },
    { level: 'Medium', q: 'Detect whether a graph is a valid tree.', a: 'It must have exactly `n - 1` edges, be connected, and contain no cycle. Given `n - 1` edges, connected implies acyclic, so checking connectivity is enough.' },
    { level: 'Hard', q: 'What is a bridge and how do you find one?', a: 'An edge whose removal disconnects the graph. DFS with entry times and low-link values: edge `(u,v)` is a bridge when `low[v] > entry[u]`. `O(V + E)`.' },
    { level: 'Hard', q: 'What is an articulation point?', a: 'A vertex whose removal disconnects the graph. Same DFS with low-link values, with a separate rule for the root: it is an articulation point if it has more than one DFS child.' },
    { level: 'Hard', q: 'What is a strongly connected component?', a: 'A maximal set of vertices where every one can reach every other. Kosaraju does two passes — one recording finish order, one on the reversed graph. Tarjan does it in a single pass with low-link values.' },
    { level: 'Medium', q: 'How do you find the shortest path in a weighted grid?', a: 'Dijkstra with the cell as the vertex, or 0-1 BFS if the weights are only 0 and 1. Plain BFS is wrong the moment the costs differ.' },
    { level: 'Medium', q: 'How do you reconstruct the actual path, not just the distance?', a: 'Store a parent pointer whenever you improve a distance, then walk backwards from the destination and reverse.' },
    { level: 'Medium', q: 'What is the difference between a walk, a path and a cycle?', a: 'A walk allows repeats; a path does not repeat vertices; a cycle is a path that returns to its start. Problems that say "path" usually forbid revisiting.' },
    { level: 'Medium', q: 'How do you handle a disconnected graph?', a: 'Loop over every vertex and start a traversal from any that is still unvisited. Forgetting this is the most common near-miss in component, bipartite and cycle questions.' },
    { level: 'Medium', q: 'Recursive DFS overflows on a large graph. What now?', a: 'Convert to an explicit stack. Depth can reach `V`, and a chain of a hundred thousand vertices exhausts the call stack even though the logic is correct.' },
    { level: 'Medium', q: 'What is an Euler path?', a: 'A walk using every edge exactly once. It exists in a connected undirected graph when zero or exactly two vertices have odd degree.' },
    { level: 'Medium', q: 'Network delay time — which algorithm?', a: 'Dijkstra from the source; the answer is the largest finite distance, or -1 if some vertex is unreachable.' },
    { level: 'Hard', q: 'Cheapest flight with at most `k` stops.', a: 'Bellman-Ford limited to `k+1` rounds, relaxing from a snapshot of the previous round so one pass cannot use more than one extra edge. Plain Dijkstra is wrong because the cheapest route may use too many stops.' },
    { level: 'Medium', q: 'What is the complexity of BFS and DFS?', a: '`O(V + E)` time and `O(V)` space. Writing it as `O(n)` hides which quantity dominates, and interviewers notice.' },
    { level: 'Medium', q: 'Alien dictionary — what is the underlying problem?', a: 'Build edges from the first differing character of each adjacent word pair, then topological sort. Watch the invalid case where a longer word precedes its own prefix.' },
  ],
};

export const GREEDY_QUESTIONS: QuestionSet = {
  topic: 'greedy',
  questions: [
    { level: 'Easy', q: 'What two properties must hold for greedy to be correct?', a: 'The greedy choice property — some optimal solution contains the choice you are making — and optimal substructure. The first is the one that fails.' },
    { level: 'Medium', q: 'What is an exchange argument?', a: 'Take any optimal solution that does not contain your greedy choice, swap your choice in, and show the result is still valid and no worse. That proves greedy is safe.' },
    { level: 'Medium', q: 'Why sort by finish time for activity selection?', a: 'Finishing earliest leaves the most room for everything after. Sorting by start can pick one long interval that blocks several short ones; sorting by duration can pick one that straddles two slots.' },
    { level: 'Medium', q: 'When do you sort intervals by start instead?', a: 'When merging overlaps or sweeping for concurrency. Then any interval that could overlap the current one begins at or after it, so a single forward scan suffices.' },
    { level: 'Medium', q: 'Minimum number of platforms or meeting rooms.', a: 'Sort by start and keep a min-heap of end times, or sort arrivals and departures separately and sweep with two pointers counting concurrent events. The peak is the answer.' },
    { level: 'Medium', q: 'Why is fractional knapsack greedy but 0/1 knapsack not?', a: 'With fractions you can always fill the capacity exactly, so the best value-per-weight first never wastes space. With whole items, taking the best ratio can leave a gap a different combination would have filled.' },
    { level: 'Medium', q: 'Give a case where greedy coin change fails.', a: 'Coins `[1, 5, 9]` and target 15: greedy takes 9 then 5 then 1, three coins, but 5+5+5 is two. No optimal solution contains the 9, so the first choice was already wrong.' },
    { level: 'Medium', q: 'Explain the gas station solution.', a: 'If the total gain is non-negative a solution exists. Scan once with a running tank, and whenever it goes negative reset the start to the next station — nothing in the failed stretch could have been a valid start.' },
    { level: 'Medium', q: 'Jump game — why is greedy enough?', a: 'Track the furthest index reachable so far. Reachability is monotone along the array, so if you can reach index `i` you can reach everything before it. `O(n)`.' },
    { level: 'Medium', q: 'Jump game II — minimum jumps.', a: 'A BFS-like sweep: keep the end of the current jump range and the furthest reachable; when you reach the end of the range, increment the jump count and extend. `O(n)`.' },
    { level: 'Medium', q: 'Explain Huffman coding.', a: 'Repeatedly merge the two least frequent symbols using a min-heap. The rarest symbols end up deepest, which minimises the weighted path length and therefore the encoded size.' },
    { level: 'Medium', q: 'Job sequencing with deadlines.', a: 'Sort by profit descending and place each job in the latest free slot before its deadline, using union-find or a boolean array to find that slot quickly.' },
    { level: 'Medium', q: 'Minimum arrows to burst balloons.', a: 'Sort by end point and shoot at the current end, skipping every balloon it hits. It is interval selection with the comparison flipped.' },
    { level: 'Medium', q: 'Non-overlapping intervals — minimum removals.', a: 'Count the maximum you can keep by sorting on end time, then subtract from `n`. Solving the complement is easier than choosing what to delete.' },
    { level: 'Medium', q: 'Partition labels — what is the greedy step?', a: 'Record the last index of each character, then scan extending the current partition end to the furthest last-index seen. Cut when the scan reaches that end.' },
    { level: 'Medium', q: 'Candy distribution with rating constraints.', a: 'Two passes: left to right giving more than the left neighbour when the rating rises, then right to left doing the same, taking the maximum at each position.' },
    { level: 'Medium', q: 'When should you suspect greedy is wrong?', a: 'When a choice now can block a better combination later, when the problem asks for a count of ways, or when it smells like knapsack. Then reach for DP.' },
    { level: 'Medium', q: 'How should you present a greedy solution in an interview?', a: 'State the rule and the reason in one clause — "sort by end time, because finishing earliest leaves the most room" — then code it. The reason is what distinguishes it from a guess.' },
    { level: 'Medium', q: 'What is the fastest way to test a greedy idea?', a: 'Spend two minutes trying to break it on a small input. Finding a counterexample is much quicker than constructing a proof, and it is how wrong rules get caught.' },
    { level: 'Medium', q: 'Which classic algorithms are greedy?', a: "Dijkstra, Prim, Kruskal, Huffman, activity selection and fractional knapsack. Each has a proof — the cut property, non-negative weights, the exchange argument." },
    { level: 'Medium', q: 'Why does Kruskal work?', a: 'The cut property: the cheapest edge crossing any partition of the vertices belongs to some MST. Kruskal only ever adds such an edge, checked with union-find.' },
    { level: 'Medium', q: 'Two city scheduling — what is the greedy key?', a: 'Sort by the difference in cost between the two options. Sending the people with the largest saving to the cheaper city is optimal, and the difference is the right sort key rather than either cost alone.' },
  ],
};
