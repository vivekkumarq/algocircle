import { QuestionSet } from './question.model';

export const ARRAYS_QUESTIONS: QuestionSet = {
  topic: 'arrays',
  questions: [
    { level: 'Easy', q: 'What are the costs of the basic array operations?', a: 'Index read or write `O(1)`; append `O(1)` amortised on a dynamic array; insert or delete at the front or middle `O(n)`; delete at the end `O(1)`; search `O(n)` unsorted, `O(log n)` sorted.' },
    { level: 'Easy', q: 'Array versus linked list — when do you pick each?', a: 'Array for indexed access, iteration speed and cache locality. Linked list when you already hold a node and need `O(1)` insertion or removal there, or when the size changes constantly at the front.' },
    { level: 'Easy', q: 'What is the difference between a subarray, a subsequence and a subset?', a: 'A subarray is contiguous (`n(n+1)/2` of them), a subsequence keeps order but may skip (`2ⁿ`), a subset ignores order (`2ⁿ`). The word "contiguous" is the strongest hint in array problems.' },
    { level: 'Easy', q: 'How do prefix sums answer range queries in constant time?', a: 'Precompute running totals with `prefix[0] = 0`. Then the sum of `a[l..r]` is `prefix[r+1] - prefix[l]` — one subtraction, after `O(n)` preprocessing.' },
    { level: 'Easy', q: 'Why is the prefix array one element longer than the input?', a: 'So `prefix[0] = 0` represents the empty prefix. That removes the special case for ranges starting at index 0, which is where nearly all prefix-sum bugs come from.' },
    { level: 'Medium', q: 'How do you count subarrays whose sum equals `k`?', a: 'Scan keeping a running sum, and a map of how many times each running sum has occurred. At each index add the count of `running - k`. Seed the map with `{0: 1}` for the empty prefix. `O(n)`.' },
    { level: 'Medium', q: 'How do you find subarrays whose sum is divisible by `k`?', a: 'Same skeleton, but store `running % k` instead of `running`. Two prefixes with the same remainder bound a subarray divisible by `k`. Normalise negative remainders by adding `k`.' },
    { level: 'Medium', q: 'What is a difference array and when do you use it?', a: 'The mirror of prefix sums: to add `v` to `a[l..r]`, do `diff[l] += v` and `diff[r+1] -= v`, then rebuild with one running sum. `q` updates cost `O(n + q)` instead of `O(n·q)`.' },
    { level: 'Medium', q: "Explain Kadane's algorithm.", a: 'At each index the best subarray ending there either extends the previous one or starts fresh: `endingHere = max(a[i], endingHere + a[i])`. Track the maximum of those values. `O(n)` time, `O(1)` space.' },
    { level: 'Medium', q: 'What breaks Kadane when all values are negative?', a: 'Initialising the best to 0 returns 0, which is only correct if the empty subarray is allowed. Start both variables at `a[0]` and the case handles itself.' },
    { level: 'Medium', q: 'How do you find the maximum product subarray?', a: 'Track both the maximum and minimum ending here, because a negative value swaps them. `newMax = max(a[i], max·a[i], min·a[i])`, and similarly for the minimum.' },
    { level: 'Medium', q: 'Move all zeroes to the end while keeping order, in place.', a: 'A write pointer: scan with `read`, and whenever `a[read]` is non-zero write it at `write++`. Then fill from `write` to the end with zeroes. `O(n)`, `O(1)`.' },
    { level: 'Medium', q: 'Remove duplicates from a sorted array in place.', a: 'Write pointer starting at 1: for each `read`, if `a[read] != a[write-1]` then `a[write++] = a[read]`. The new length is `write`.' },
    { level: 'Medium', q: 'Why is the write-pointer pattern safe?', a: 'The write pointer never overtakes the read pointer, so you only ever overwrite a slot you have already consumed. That invariant is the whole proof.' },
    { level: 'Medium', q: 'Rotate an array left by `k` in place.', a: 'Take `k %= n`, then reverse `[0, k)`, reverse `[k, n)`, and reverse the whole array. `O(n)` time, `O(1)` space.' },
    { level: 'Easy', q: 'What happens if you forget `k %= n` in a rotation?', a: 'A rotation by `n` or more indexes out of bounds. It is the fastest way to fail a hidden test.' },
    { level: 'Medium', q: 'Find the majority element appearing more than `n/2` times.', a: "Boyer-Moore voting: keep a candidate and a count; increment on a match, decrement otherwise, and replace the candidate at zero. `O(n)` time, `O(1)` space — then verify with a second pass." },
    { level: 'Medium', q: 'Why does Boyer-Moore voting work?', a: 'Each cancellation removes one occurrence of the majority element and one of something else. Since the majority appears more than half the time, it cannot be cancelled out entirely.' },
    { level: 'Medium', q: 'Find the two numbers that sum to a target in a sorted array.', a: 'Two pointers at both ends. If the sum is too small move `lo` right, too big move `hi` left. Each move discards a value that cannot participate in any solution. `O(n)`.' },
    { level: 'Medium', q: 'Same question, unsorted array, indices required.', a: 'One pass with a hash map from value to index, checking for `target - a[i]` before inserting `a[i]`. Sorting would destroy the original indices.' },
    { level: 'Medium', q: 'Find all triplets summing to zero without duplicates.', a: 'Sort, fix the first element with an outer loop, two-point the suffix. Skip duplicate anchors, and skip duplicate values on both sides after recording a hit. `O(n²)`.' },
    { level: 'Medium', q: 'Product of array except self, without division.', a: 'One pass accumulating the prefix product into the output, then a second pass from the right multiplying by a running suffix product. `O(n)` time, `O(1)` extra space beyond the output.' },
    { level: 'Medium', q: 'Why avoid division in "product except self"?', a: 'A single zero makes division undefined for every other position, and two zeroes make the whole answer zero. The prefix and suffix version handles both without special cases.' },
    { level: 'Medium', q: 'Find the missing number in `0..n`.', a: 'XOR every index with every value, or subtract the actual sum from `n(n+1)/2`. XOR avoids any overflow concern.' },
    { level: 'Medium', q: 'Find a duplicate in an array of `n+1` values in `1..n` without modifying it.', a: "Treat values as next pointers and use Floyd's cycle detection: the duplicate is the entry point of the cycle. `O(n)` time, `O(1)` space." },
    { level: 'Medium', q: 'Sort an array of 0s, 1s and 2s in one pass.', a: 'Dutch national flag with `low`, `mid`, `high`. On a 0 swap with `low` and advance both; on a 1 advance `mid`; on a 2 swap with `high` and decrement `high` **without** advancing `mid`.' },
    { level: 'Medium', q: 'Why does `mid` not advance after swapping with `high`?', a: 'The value that arrived from the right has not been examined yet. Advancing skips it and produces a wrong ordering.' },
    { level: 'Medium', q: 'Merge overlapping intervals.', a: 'Sort by start, then scan: if the current interval starts at or before the end of the last merged one, extend that end; otherwise append a new interval. `O(n log n)`.' },
    { level: 'Medium', q: 'Find the maximum in every window of size `k`.', a: 'A monotonic deque of indices with decreasing values. Drop smaller values from the back before pushing, drop the front when it slides out. Each index enters and leaves once, so `O(n)`.' },
    { level: 'Medium', q: 'How do you rotate a matrix 90 degrees clockwise in place?', a: 'Transpose it, then reverse each row. Reversing each column instead rotates anticlockwise.' },
    { level: 'Medium', q: 'How does a 2D prefix sum answer rectangle queries?', a: 'Build `P[r+1][c+1] = grid[r][c] + P[r][c+1] + P[r+1][c] - P[r][c]`. A rectangle is then four lookups by inclusion and exclusion, in `O(1)`.' },
    { level: 'Easy', q: 'How do you flatten a matrix index and get it back?', a: '`index = row * cols + col`; `row = index / cols`, `col = index % cols`. It lets you binary search a row-sorted matrix as one array.' },
    { level: 'Easy', q: 'What is the direction-array idiom?', a: '`dr = {-1,1,0,0}`, `dc = {0,0,-1,1}` and a loop over the four offsets, with a bounds check. It removes four near-identical branches and reappears in every grid BFS.' },
    { level: 'Medium', q: 'Spiral order traversal of a matrix — what is the trick?', a: 'Keep four boundaries — top, bottom, left, right — and shrink one after each pass. Check that the boundaries have not crossed before the reverse passes, or you re-read a row.' },
    { level: 'Medium', q: 'Set matrix zeroes with `O(1)` extra space.', a: 'Use the first row and first column as the flags themselves, with one extra variable for whether the first column originally contained a zero. Write back in reverse order so the flags are read before being overwritten.' },
    { level: 'Medium', q: 'Trapping rain water — the two-pointer solution.', a: 'Move whichever side has the smaller wall inward, keeping `leftMax` and `rightMax`. Water above the current bar is `min(leftMax, rightMax) - height`, and the smaller side is always the binding constraint. `O(n)`, `O(1)`.' },
    { level: 'Medium', q: 'Best time to buy and sell stock once.', a: 'One pass tracking the minimum price so far and the best profit if you sold today. `O(n)` time, `O(1)` space.' },
    { level: 'Easy', q: 'When is a frequency array better than a hash map?', a: 'When the value range is small, dense and known — letters, digits, ages, values up to a million. No hashing cost, no collisions, and perfect cache behaviour.' },
    { level: 'Hard', q: 'Find the longest consecutive sequence in an unsorted array in `O(n)`.', a: 'Put everything in a hash set. For each value, only start counting if `value - 1` is absent — that makes each run walked exactly once, so the total stays linear despite the inner loop.' },
    { level: 'Medium', q: 'What single question unlocks most linear array techniques?', a: '"What am I recomputing that I already knew one step ago?" Prefix sums, Kadane, sliding windows and running maxima are all answers to it.' },
  ],
};
