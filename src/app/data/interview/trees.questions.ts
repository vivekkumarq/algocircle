import { QuestionSet } from './question.model';

export const STACKS_QUEUES_QUESTIONS: QuestionSet = {
  topic: 'stacks-queues',
  questions: [
    { level: 'Easy', q: 'When do you reach for a stack rather than a queue?', a: 'A stack whenever the problem is nested or "most recent unfinished thing": brackets, expressions, undo, DFS. A queue whenever it is about fairness or distance from the start: scheduling, BFS, level order.' },
    { level: 'Easy', q: 'Why prefer `ArrayDeque` over `Stack` in Java?', a: '`Stack` extends `Vector`, so it is synchronised, slower, and iterates bottom-to-top which surprises people. `ArrayDeque` serves as both stack and queue and is faster.' },
    { level: 'Easy', q: 'Validate a string of brackets.', a: 'Push openers; on a closer, fail if the stack is empty or the top does not match. At the end the stack must be empty. Both checks are needed — `"("` and `")("` each catch only one.' },
    { level: 'Medium', q: 'Evaluate a postfix expression.', a: 'Push operands; on an operator pop two and apply. The **second** value popped is the left operand — getting that backwards passes for `+` and `*` and silently fails for `-` and `/`.' },
    { level: 'Medium', q: 'Why do compilers convert infix to postfix?', a: 'Postfix needs no brackets and no precedence table, so evaluation is a five-line stack loop instead of a parser.' },
    { level: 'Medium', q: 'What is a monotonic stack?', a: 'A stack whose values stay sorted. An arriving element pops everything it dominates, and each popped element learns its nearest greater or smaller neighbour. `O(n)` overall.' },
    { level: 'Medium', q: 'Why is a monotonic stack linear despite the inner `while`?', a: 'Each index is pushed exactly once and popped at most once, so total pops are bounded by `n`. A long inner loop is paid for by short ones elsewhere.' },
    { level: 'Medium', q: 'Next greater element — write the shape.', a: 'Scan left to right with a stack of indices; while the top value is smaller than the current, pop and record the current as its answer; then push the current index.' },
    { level: 'Medium', q: 'How do you get previous smaller instead of next greater?', a: 'Same scan, but pop while the top is greater than or equal to the current, and then **peek** — the remaining top is the previous smaller element.' },
    { level: 'Hard', q: 'Largest rectangle in a histogram.', a: 'For each bar, the widest rectangle extends to the previous and next strictly shorter bars. A monotonic increasing stack supplies both boundaries; a sentinel of height 0 at the end flushes the stack. `O(n)`.' },
    { level: 'Hard', q: 'Maximal rectangle in a binary matrix.', a: 'Treat each row as a histogram of consecutive ones above it and run the histogram routine once per row. `O(rows × cols)`.' },
    { level: 'Medium', q: 'Trapping rain water with a stack.', a: 'Keep a decreasing stack of indices. When a taller bar arrives, pop the bottom of a basin, and the water trapped is the width between the new bar and the new top, times the height difference.' },
    { level: 'Medium', q: 'Design a stack with `getMin` in `O(1)`.', a: 'Store pairs of value and minimum-so-far, or keep a second stack of minima. Either way the minimum is available at the top without scanning.' },
    { level: 'Medium', q: 'Implement a queue using two stacks.', a: 'Push onto the input stack. To pop, if the output stack is empty move everything across, then pop from it. Each element moves once, so it is `O(1)` amortised.' },
    { level: 'Medium', q: 'Implement a stack using two queues.', a: 'Make one operation expensive: either push in `O(n)` by rotating the queue so the new element is at the front, or pop in `O(n)` by moving all but the last element across.' },
    { level: 'Medium', q: 'What is a circular queue and why use one?', a: 'A fixed array with head and tail indices wrapped by a modulus, so both ends are `O(1)` and memory is reused. Track the size explicitly, or full and empty look identical.' },
    { level: 'Medium', q: 'What is a monotonic deque used for?', a: 'The maximum or minimum inside a sliding window. A deque can also discard from the front when an element leaves the window, which a stack cannot.' },
    { level: 'Medium', q: 'Stock span problem.', a: 'For each day, how many consecutive previous days had a price at most today\'s — that is "previous greater element", answered by one monotonic stack pass.' },
    { level: 'Medium', q: 'Asteroid collision — why a stack?', a: 'A right-moving asteroid may collide with several later left-moving ones, resolving most-recent-first. That is exactly stack order.' },
    { level: 'Medium', q: 'Remove `k` digits to make the smallest number.', a: 'A monotonic increasing stack: pop larger digits while removals remain, then trim any leftover from the end and strip leading zeroes.' },
    { level: 'Medium', q: 'Decode a nested string like `3[a2[c]]`.', a: 'Two stacks — one for repeat counts, one for partial strings — or one stack of pairs. On `[` push the state, on `]` pop and repeat the accumulated fragment.' },
    { level: 'Medium', q: 'What is a sentinel and why use one here?', a: 'A virtual element appended so the main loop handles the tail case. In the histogram problem a bar of height 0 at the end flushes the stack, removing a separate cleanup loop.' },
    { level: 'Medium', q: 'How would you implement a browser back and forward?', a: 'Two stacks. Visiting a page pushes onto back and clears forward; going back moves the current page onto forward and pops from back.' },
    { level: 'Medium', q: 'Design a hit counter for the last five minutes.', a: 'A queue of timestamps: on each hit enqueue the time and drop from the front while entries are older than the window. The size is the count.' },
    { level: 'Medium', q: 'Which traversal uses a stack and which uses a queue?', a: 'DFS uses a stack (explicit or the call stack); BFS uses a queue. That single difference is what makes BFS give shortest paths and DFS give structure.' },
    { level: 'Medium', q: 'How do you know a problem needs "nearest greater" thinking?', a: 'Phrases like next greater, previous smaller, span, warmer temperature, visible buildings, or largest rectangle — anything about the nearest element on one side satisfying a comparison.' },
    { level: 'Medium', q: 'What is the space complexity of a monotonic stack solution?', a: '`O(n)` in the worst case, when the input is already sorted so nothing is ever popped until the end.' },
    { level: 'Easy', q: 'Where does the call stack fit into this topic?', a: 'It is literally a stack of frames. That is why recursion and DFS are interchangeable, and why converting recursion to iteration means managing the stack yourself.' },
  ],
};

export const TREES_QUESTIONS: QuestionSet = {
  topic: 'trees',
  questions: [
    { level: 'Easy', q: 'What is the difference between height and depth?', a: 'Depth is measured from the root down to a node; height is measured from a node down to its deepest leaf. The height of the tree is the height of the root.' },
    { level: 'Easy', q: 'How many edges does a tree with `n` nodes have?', a: 'Exactly `n - 1`. A connected graph with fewer is impossible and with more contains a cycle.' },
    { level: 'Easy', q: 'What is the difference between a complete, a full and a balanced tree?', a: 'Complete: every level full except possibly the last, filled left to right. Full: every node has zero or two children. Balanced: left and right heights differ by at most one at every node.' },
    { level: 'Easy', q: 'Name the four traversal orders and what each is for.', a: 'Preorder for copying and serialising, inorder for sorted output from a BST, postorder for bottom-up answers and deletion, level order for per-level work and shortest depth.' },
    { level: 'Medium', q: 'Write iterative inorder traversal.', a: 'Push left children while descending; on pop, visit the node and move to its right child. The stack replaces the call frames.' },
    { level: 'Medium', q: 'In an iterative preorder, why push the right child first?', a: 'A stack is last-in first-out, so pushing right then left makes the left subtree pop first, matching the recursive order.' },
    { level: 'Medium', q: 'How do you keep levels separate in a level-order traversal?', a: 'Take `queue.size()` into a variable **before** the inner loop and process exactly that many nodes. Reading it inside the loop mixes the next level in.' },
    { level: 'Medium', q: 'Compute the height of a binary tree.', a: 'Postorder: `height(node) = 1 + max(height(left), height(right))`, with an empty tree returning -1 if you count edges (or 0 if you count nodes — state which).' },
    { level: 'Medium', q: 'Compute the diameter.', a: 'Return the height to the parent while recording `left + right + 2` as a candidate global best at each node. One pass, `O(n)`.' },
    { level: 'Medium', q: 'Why is a naive `isBalanced` `O(n²)`?', a: 'It calls `height` at every node, recomputing the subtree heights. Returning height and the balance verdict together in one postorder pass makes it `O(n)`.' },
    { level: 'Medium', q: 'How do you validate a BST?', a: 'Carry a permitted `(min, max)` range down the recursion, or check that an inorder traversal is strictly increasing. Comparing only against the immediate children is wrong.' },
    { level: 'Medium', q: 'Why is the local-comparison BST check wrong?', a: 'It accepts trees where a deep left descendant is larger than an ancestor, because the constraint is global — every node in the left subtree must be smaller, not just the child.' },
    { level: 'Medium', q: 'Find the lowest common ancestor in a general binary tree.', a: 'Recurse; return the node if it is one of the targets. If both sides return non-null, this node is the LCA; otherwise pass up whichever side found something.' },
    { level: 'Medium', q: 'Find the LCA in a BST — what is simpler?', a: 'Walk down from the root: go left while both targets are smaller, right while both are larger. The first node that splits them is the LCA. `O(height)`, no recursion needed.' },
    { level: 'Medium', q: 'Find the kth smallest element in a BST.', a: 'Inorder traversal, stopping at the kth visit. With frequent queries, store subtree sizes so each query is `O(height)`.' },
    { level: 'Medium', q: 'How do you delete a node from a BST?', a: 'A leaf is removed outright; a node with one child is replaced by that child; a node with two children is replaced by its inorder successor, which is then deleted from the right subtree.' },
    { level: 'Medium', q: 'What is the inorder successor of a node?', a: 'The leftmost node of its right subtree if one exists; otherwise the last ancestor at which you turned left on the way down.' },
    { level: 'Medium', q: 'Serialise and deserialise a binary tree.', a: 'Preorder with explicit null markers is self-sufficient: read the root, then recursively read the left and right subtrees. Level order with markers also works.' },
    { level: 'Medium', q: 'Which pairs of traversals uniquely determine a tree?', a: 'Preorder plus inorder, or postorder plus inorder. Preorder plus postorder is ambiguous for general binary trees.' },
    { level: 'Medium', q: 'Why can preorder plus inorder rebuild a tree?', a: 'Preorder gives the root; its position in the inorder sequence says how many nodes fall in each subtree. Index the inorder positions in a map first, or the rebuild is `O(n²)`.' },
    { level: 'Medium', q: 'How do you compute the left or right view?', a: 'Level order, taking the first or last node of each level. A DFS tracking maximum depth seen so far works too.' },
    { level: 'Medium', q: 'What is horizontal distance and what does it unlock?', a: 'Root at 0, left child `d-1`, right child `d+1`. Top view keeps the first node seen per distance, bottom view the last, and vertical order groups by it.' },
    { level: 'Medium', q: 'Boundary traversal — what are the three parts?', a: 'The left boundary top-down excluding leaves, then all leaves left to right, then the right boundary bottom-up excluding leaves. Handle the root and single-child cases carefully.' },
    { level: 'Hard', q: 'Maximum path sum in a binary tree.', a: 'Each node returns the best **downward** path through it, clamping negative branches to zero. The global best considers `node + left + right`, the path that turns at that node.' },
    { level: 'Medium', q: 'Why clamp negative branches to zero?', a: 'Because a branch with a negative total can always be omitted — including it can only reduce the sum, and omitting it is always allowed.' },
    { level: 'Medium', q: 'Print all root-to-leaf paths.', a: 'Carry the path down, appending on entry and removing on exit. Record a copy at each leaf, not the live list.' },
    { level: 'Medium', q: 'Check whether two trees are identical, and whether one is a subtree of the other.', a: 'Identical is a simultaneous recursion comparing structure and values. Subtree is: at every node of the larger tree, test identical — `O(n·m)` naively, or `O(n+m)` by serialising with markers and doing a substring search.' },
    { level: 'Medium', q: 'Convert a sorted array into a balanced BST.', a: 'Take the middle element as the root and recurse on both halves. `O(n)`, and the balance is automatic.' },
    { level: 'Medium', q: 'What is a threaded binary tree?', a: 'One where null child pointers are reused to point at the inorder predecessor or successor, allowing traversal without recursion or a stack. Morris traversal builds these links temporarily.' },
    { level: 'Hard', q: 'Explain Morris traversal.', a: 'Inorder in `O(1)` space: link the rightmost node of the left subtree back to the current node, descend, and remove the link when you return through it. It mutates the tree temporarily.' },
    { level: 'Medium', q: 'What is the time complexity of BST search, and what is the catch?', a: '`O(height)` — `O(log n)` when balanced, `O(n)` when skewed. Every "`O(log n)`" claim about a BST quietly assumes balance.' },
    { level: 'Medium', q: 'What do AVL and red-black trees give you?', a: 'A guaranteed logarithmic height through rotations. AVL is more strictly balanced so lookups are faster; red-black rotates less so updates are cheaper, which is why libraries use it.' },
    { level: 'Medium', q: 'Count nodes in a complete binary tree faster than `O(n)`.', a: 'Compare the left and right spine heights. If they match, the subtree is perfect and the count is `2^h - 1`; otherwise recurse. That gives `O(log²n)`.' },
    { level: 'Medium', q: 'Find all nodes at distance `k` from a target node.', a: 'Build parent links (or a graph), then BFS outward from the target for `k` levels, tracking visited nodes so you do not walk back.' },
    { level: 'Medium', q: 'How do you find the width of a binary tree?', a: 'Level order with position indices — left child `2i`, right child `2i+1` — and take the difference between the first and last index on each level. Normalise per level to avoid overflow.' },
    { level: 'Medium', q: 'Flatten a binary tree to a linked list in preorder.', a: 'For each node, move the left subtree into the right position and attach the original right subtree to the rightmost node of the moved section. `O(1)` extra space.' },
    { level: 'Medium', q: 'When would you use a BST over a hash map?', a: 'When you need sorted iteration, range queries, or floor and ceiling lookups — and when you need a guaranteed `O(log n)` rather than an average `O(1)`.' },
    { level: 'Medium', q: 'What is the recursion depth of a tree algorithm?', a: 'The height. That is `O(log n)` on a balanced tree and `O(n)` on a skewed one, which is the honest answer to "what is the space complexity".' },
    { level: 'Medium', q: 'What single question solves most tree problems?', a: '"What does this node need from its children, and what does it owe its parent?" Answer that and the code is usually five lines.' },
    { level: 'Medium', q: 'Sum of nodes on the longest root-to-leaf path.', a: 'Postorder returning both depth and sum, preferring the deeper branch and breaking ties by the larger sum. It is the return-one-thing, record-another pattern again.' },
  ],
};

export const HEAPS_QUESTIONS: QuestionSet = {
  topic: 'heaps',
  questions: [
    { level: 'Easy', q: 'What is the heap property?', a: 'In a min-heap every node is less than or equal to both children. Nothing is promised about siblings or left versus right — that weaker promise is why maintenance is cheap.' },
    { level: 'Easy', q: 'Why can a heap live in a plain array?', a: 'It is a complete tree, so positions are implied: parent `(i-1)/2`, children `2i+1` and `2i+2`. No pointers, no per-node allocation, and contiguous memory.' },
    { level: 'Easy', q: 'What are the costs of heap operations?', a: 'Peek `O(1)`, insert `O(log n)`, extract-min `O(log n)`, build from an array `O(n)`, and search for an arbitrary value `O(n)` because there is no ordering to guide it.' },
    { level: 'Medium', q: 'Why is building a heap `O(n)`?', a: 'Sifting down from the last internal node backwards: half the nodes are leaves and do nothing, a quarter sift at most one level, an eighth at most two. The series sums to less than `n`.' },
    { level: 'Medium', q: 'Why sift down rather than up when heapifying?', a: 'Sifting down makes the many cheap nodes the leaves. Sifting up would put the expensive work on the leaves, which are the majority, giving `O(n log n)`.' },
    { level: 'Medium', q: 'What is the common bug in sift-down?', a: 'Swapping with the left child unconditionally. You must compare against the **smaller** of the two children, or the property breaks on the other side.' },
    { level: 'Easy', q: 'Which default does your language give — min or max?', a: 'Java `PriorityQueue` is a min-heap; C++ `priority_queue` is a max-heap; Python `heapq` is a min-heap. Getting it backwards is a silent wrong answer, not a compile error.' },
    { level: 'Medium', q: 'For the `k` largest elements, which heap do you use?', a: 'A **min**-heap of size `k`. Its root is the weakest current champion, so it is exactly the element to evict when something better arrives. `O(n log k)`.' },
    { level: 'Medium', q: 'When is quickselect better than a heap for kth largest?', a: 'When the whole array is in memory and you only need one query: expected `O(n)` beats `O(n log k)`. The heap wins on streams, or when `k` is small and `n` is unknown.' },
    { level: 'Medium', q: 'Merge `k` sorted lists with a heap.', a: 'Push one candidate per list, pop the smallest, append it, then push that list\'s next element. The heap never exceeds `k`, giving `O(n log k)`.' },
    { level: 'Hard', q: 'Maintain the median of a stream.', a: 'A max-heap for the lower half and a min-heap for the upper half, kept within one element of each other. The median is the larger root, or the average of both roots. `O(log n)` per insert.' },
    { level: 'Medium', q: 'How do you rebalance the two heaps safely?', a: 'Always push into the lower heap, immediately move its maximum into the upper heap, and move back if the upper becomes larger. That avoids comparing against a median that may not exist yet.' },
    { level: 'Medium', q: 'Top `k` frequent elements — heap or buckets?', a: 'A heap of size `k` gives `O(n log k)`. Bucketing by frequency into an array of lists gives `O(n)`, since frequencies are bounded by `n`.' },
    { level: 'Medium', q: 'Minimum meeting rooms.', a: 'Sort by start, keep a min-heap of end times. If the earliest end is at or before the next start, reuse that room by popping. The heap size at the end is the answer.' },
    { level: 'Medium', q: 'Connect ropes at minimum cost.', a: 'Always join the two shortest, pushing the result back. A min-heap makes each step `O(log n)`; the greedy choice is safe because the earliest joins are counted most often.' },
    { level: 'Medium', q: 'Task scheduler with a cooldown.', a: 'A max-heap by remaining count, taking up to `n+1` distinct tasks per round and pushing back whatever still has work. Alternatively compute the answer directly from the most frequent task.' },
    { level: 'Medium', q: 'Kth largest in a stream — design the class.', a: 'Keep a min-heap of size `k`. `add` pushes and pops when the size exceeds `k`; the root is the answer. `O(log k)` per insert.' },
    { level: 'Medium', q: 'Find the `k` closest points to the origin.', a: 'A max-heap of size `k` keyed on squared distance — no square root needed — evicting the farthest. `O(n log k)`. Quickselect gives expected `O(n)`.' },
    { level: 'Medium', q: 'Where do heaps appear inside other algorithms?', a: "Dijkstra's frontier, Prim's cheapest crossing edge, Huffman's two least frequent symbols, and any greedy that needs the current best repeatedly." },
    { level: 'Medium', q: 'How do you decrease a key in a heap?', a: 'Standard library heaps do not support it. The usual workaround is lazy deletion: push the new entry and skip stale ones on pop by checking against the best known value.' },
    { level: 'Medium', q: 'What is heap sort and what is its trade-off?', a: 'Build a heap in `O(n)`, then extract the maximum `n` times into the tail of the array. `O(n log n)` guaranteed with `O(1)` space, but unstable and less cache-friendly than quick sort.' },
    { level: 'Medium', q: 'Is a heap a sorted structure?', a: 'No. Only the root is guaranteed. Iterating the array gives no meaningful order, which is why searching a heap is `O(n)`.' },
    { level: 'Medium', q: 'When would a balanced BST beat a heap?', a: 'When you need ordered traversal, both extremes, predecessor and successor, or arbitrary deletion. A heap gives you one extreme very cheaply and nothing else.' },
    { level: 'Medium', q: 'Sort a nearly sorted array where each element is at most `k` out of place.', a: 'A min-heap of size `k+1`: push as you scan and pop the minimum each step. `O(n log k)`.' },
    { level: 'Medium', q: 'What signals a heap problem?', a: '"Top K", "kth largest", "median of a stream", "merge k", "schedule", or any phrase meaning "I repeatedly need the current best".' },
  ],
};
