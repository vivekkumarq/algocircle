import { QuestionSet } from './question.model';

export const COMPLEXITY_QUESTIONS: QuestionSet = {
  topic: 'complexity',
  questions: [
    { level: 'Easy', q: 'What is Big-O notation?', a: 'An upper bound on how the cost grows with input size. Formally `f(n) = O(g(n))` if there are constants `c` and `n0` with `f(n) <= c·g(n)` for all `n >= n0` — past some size, `f` never grows faster than `g` scaled by a constant.' },
    { level: 'Easy', q: 'What is the difference between Big-O, Big-Omega and Big-Theta?', a: 'O is an upper bound (at most), Omega is a lower bound (at least), Theta is both, so it is the tight description of the growth.' },
    { level: 'Easy', q: 'Why do we drop constants and lower-order terms?', a: 'Constants depend on the machine, language and compiler — exactly what we are abstracting away. Lower-order terms are dominated as `n` grows, so neither can change the shape of the curve.' },
    { level: 'Easy', q: 'Order these from fastest to slowest growth: n², log n, n log n, 1, n, 2ⁿ.', a: '`1`, `log n`, `n`, `n log n`, `n²`, `2ⁿ`.' },
    { level: 'Easy', q: 'What is the complexity of two sequential loops over the same array?', a: '`O(n)`. Sequential blocks add, and `O(n + n) = O(2n) = O(n)`. Only nesting multiplies.' },
    { level: 'Medium', q: 'What is the complexity of a loop where `i` doubles each iteration?', a: '`O(log n)`, because `i` reaches `n` after about `log2(n)` doublings.' },
    { level: 'Medium', q: 'What is the complexity of an inner loop that runs from `i+1` to `n`?', a: 'Still `O(n²)`. It performs about `n²/2` iterations, and constants are dropped.' },
    { level: 'Easy', q: 'When should you quote the worst case rather than the average?', a: 'Always, unless asked otherwise — it is the only guarantee. Mention the average when it genuinely differs and matters, as with quick sort and hash maps.' },
    { level: 'Medium', q: 'What is amortised complexity?', a: 'The average cost per operation across a sequence, when a rare operation is expensive. Appending to a dynamic array is `O(1)` amortised: doubling means `n` appends cost under `2n` copies in total.' },
    { level: 'Medium', q: 'Why does a dynamic array double its capacity rather than grow by a fixed amount?', a: 'Doubling makes the total copying across `n` appends a geometric series bounded by `2n`, giving `O(1)` amortised. Growing by a constant would copy every few appends and make `n` appends `O(n²)`.' },
    { level: 'Medium', q: 'Is a hash map lookup `O(1)`?', a: 'On average, assuming a good hash function and reasonable load factor. The worst case is `O(n)` when every key collides into one bucket.' },
    { level: 'Easy', q: 'What is space complexity, and does the input count?', a: 'The extra memory beyond the input — auxiliary space. The input itself is normally excluded because you were given it either way.' },
    { level: 'Medium', q: 'Does a recursive function with no allocations use constant space?', a: 'No. It uses `O(depth)` stack space for the call frames — `O(log n)` on a balanced tree, `O(n)` on a skewed one.' },
    { level: 'Medium', q: 'Solve `T(n) = 2T(n/2) + O(n)`.', a: '`O(n log n)`. Each of the `log n` levels of the recursion tree does `O(n)` total work. This is merge sort.' },
    { level: 'Medium', q: 'Solve `T(n) = T(n/2) + O(1)`.', a: '`O(log n)` — binary search. Each step halves the problem and does constant work.' },
    { level: 'Medium', q: 'Solve `T(n) = 2T(n-1) + O(1)`.', a: '`O(2ⁿ)`. The tree doubles at every level and there are `n` levels — naive Fibonacci and subset enumeration.' },
    { level: 'Hard', q: 'State the master theorem informally.', a: 'For `T(n) = a·T(n/b) + f(n)`, compare `f(n)` with `n^(log_b a)`. If the recursive work dominates, that term is the answer; if `f(n)` dominates, `f(n)` is; if they match, multiply by `log n`.' },
    { level: 'Medium', q: 'The constraint says `n <= 10^5`. What complexity should you aim for?', a: '`O(n log n)` or `O(n)`. `n²` would be `10^10` operations, far beyond a typical one-second budget.' },
    { level: 'Medium', q: 'The constraint says `n <= 20`. What does that suggest?', a: 'Something exponential is intended — `2^20` is about a million. Think subsets, bitmask DP or meet in the middle.' },
    { level: 'Medium', q: 'The constraint says `n <= 10^9`. What does that suggest?', a: 'You cannot even touch every value, so the answer is mathematical: `O(log n)` with binary search on the answer, fast exponentiation, or a closed-form formula.' },
    { level: 'Easy', q: 'Roughly how many simple operations per second can you assume?', a: 'About 10^8 for ordinary code. It is a rule of thumb for deciding whether an approach fits the time limit, not a precise figure.' },
    { level: 'Medium', q: 'What hidden costs do people miss when analysing their code?', a: 'Library calls inside loops: `list.contains` is `O(n)`, `substring` may copy, `insert(0, x)` shifts everything. A loop that looks linear becomes quadratic through one innocuous call.' },
    { level: 'Medium', q: 'Sorting `n` strings of length `k` — what is the complexity?', a: '`O(n·k·log n)`, because each comparison costs up to `k`. Dropping the `k` is a common mistake since it is not a constant.' },
    { level: 'Easy', q: 'What is `log2(1,000,000)` approximately, and why does it matter?', a: 'About 20. It is why binary search over a million items takes twenty comparisons, and why logarithmic factors are almost free.' },
    { level: 'Medium', q: 'Compare `O(log n)` and `O(n)` when the input doubles.', a: 'The logarithmic algorithm does one extra step; the linear one does twice as much work. That gap is what makes sorted data and balanced trees so valuable.' },
    { level: 'Hard', q: 'Why is the monotonic stack pattern `O(n)` when it contains a nested `while`?', a: 'Amortised counting: each index is pushed exactly once and popped at most once, so the total number of pops across the whole run is at most `n`. A long inner loop on one iteration is paid for by short ones elsewhere.' },
    { level: 'Medium', q: 'Why is building a heap `O(n)` rather than `O(n log n)`?', a: 'Sifting down from the last internal node backwards, half the nodes are leaves and do no work, a quarter sift at most one level, and so on. The series `n/4·1 + n/8·2 + ...` converges to less than `n`.' },
    { level: 'Medium', q: 'What complexity does the graph size `V` and `E` imply for BFS?', a: '`O(V + E)`: every vertex is dequeued once and every edge is examined once (twice in an undirected graph). Writing it as `O(n)` hides which quantity dominates.' },
    { level: 'Easy', q: 'Is `O(2n)` different from `O(n)`?', a: 'No — they are the same class. Three passes over the data is still linear.' },
    { level: 'Hard', q: 'Your memoised solution is still too slow. What is usually wrong?', a: 'The state has too many dimensions, or one dimension has too large a range. Count the states and multiply by the work per transition; if that exceeds the budget, the state needs redesigning rather than micro-optimising.' },
  ],
};

export const MATHEMATICS_QUESTIONS: QuestionSet = {
  topic: 'mathematics',
  questions: [
    { level: 'Easy', q: 'How do you find all divisors of `n` efficiently?', a: 'Loop `d` from 1 while `d*d <= n`; when `d` divides `n`, record both `d` and `n/d`. That is `O(sqrt n)` because divisors come in pairs mirrored around the square root.' },
    { level: 'Easy', q: 'Why write the loop condition as `d*d <= n` instead of `d <= sqrt(n)`?', a: '`sqrt` returns a floating-point value, and rounding at the boundary can silently drop a divisor. Integer multiplication is exact — just cast to 64-bit to avoid overflow.' },
    { level: 'Easy', q: 'Explain the Euclidean algorithm.', a: '`gcd(a, b) = gcd(b, a mod b)`, with `gcd(a, 0) = a`. It works because any common divisor of `a` and `b` also divides `a mod b`, and it runs in `O(log min(a,b))` because two steps at least halve the larger value.' },
    { level: 'Easy', q: 'How do you compute LCM from GCD?', a: '`lcm(a,b) = a / gcd(a,b) * b`. Divide before multiplying, or the intermediate product can overflow.' },
    { level: 'Medium', q: 'How does the Sieve of Eratosthenes work and what does it cost?', a: 'Mark the multiples of each prime starting at `p*p`, since smaller multiples already have a smaller prime factor. It is about `O(n log log n)` and gives every prime below `n` in one pass.' },
    { level: 'Medium', q: 'How do you factorise many numbers quickly?', a: 'Store the smallest prime factor for each value while sieving. Then factorising any number is repeated division by its smallest prime factor, in `O(log n)` per query.' },
    { level: 'Easy', q: 'How do you test whether a single number is prime?', a: 'Handle `n < 2` and even numbers, then test odd divisors while `d*d <= n`. That is `O(sqrt n)`.' },
    { level: 'Medium', q: 'Why do problems ask for the answer modulo 10^9 + 7?', a: 'Exact answers overflow long before the input is large. That modulus is prime, which makes modular inverses easy, and the product of two values below it still fits in a signed 64-bit integer.' },
    { level: 'Medium', q: 'What goes wrong with `(a - b) % m` in Java or C++?', a: 'The result can be negative, because those languages truncate toward zero. Write `((a - b) % m + m) % m`.' },
    { level: 'Medium', q: 'Explain binary exponentiation.', a: '`a^b = (a^(b/2))²`, with one extra factor of `a` when `b` is odd. Squaring repeatedly and multiplying in the set bits of `b` gives `O(log b)` instead of `O(b)`.' },
    { level: 'Medium', q: 'How do you divide under a prime modulus?', a: 'Multiply by the modular inverse. By Fermat\'s little theorem the inverse of `a` is `a^(m-2) mod m` when `m` is prime, computed with binary exponentiation.' },
    { level: 'Easy', q: 'How many subsets does a set of `n` elements have, and why?', a: '`2ⁿ`. Each element is independently in or out, giving two choices per element. That is also why an `n`-bit integer represents exactly one subset.' },
    { level: 'Easy', q: 'What is the difference between a permutation and a combination?', a: 'A permutation counts arrangements where order matters (`n!/(n-r)!`); a combination counts selections where it does not (`n!/(r!(n-r)!)`).' },
    { level: 'Medium', q: 'How do you compute binomial coefficients without division or overflow?', a: "Use Pascal's rule `C(n,r) = C(n-1,r-1) + C(n-1,r)` and fill a table, taking the modulus as you go. It is your first DP recurrence as well." },
    { level: 'Easy', q: 'What does `n & (n - 1)` do?', a: 'Clears the lowest set bit. Subtracting one flips that bit to 0 and sets every bit below it, so the AND keeps only the higher bits. Repeating it counts set bits in as many steps as there are ones.' },
    { level: 'Easy', q: 'How do you test whether `n` is a power of two?', a: '`n > 0 && (n & (n - 1)) == 0` — a power of two has exactly one set bit, so clearing it leaves zero.' },
    { level: 'Easy', q: 'How do you isolate the lowest set bit?', a: '`n & -n`. Two\'s complement negation flips everything above the lowest set bit, so the AND leaves just that bit. Fenwick trees are built on this.' },
    { level: 'Easy', q: 'State the four XOR properties that make it useful.', a: '`a ^ a = 0`, `a ^ 0 = a`, it is commutative and associative, and it is its own inverse. Together they mean duplicates cancel regardless of order.' },
    { level: 'Medium', q: 'Every value appears twice except one. Find it in `O(1)` space.', a: 'XOR everything. The pairs cancel to zero and the lone value survives.' },
    { level: 'Hard', q: 'Every value appears twice except two. Find both in linear time and constant space.', a: 'XOR everything to get `x ^ y`. Take any set bit — say the lowest, `d = r & -r` — because that is a bit where `x` and `y` differ. Split the array on that bit and XOR each group separately.' },
    { level: 'Medium', q: 'How do you find the missing number in `0..n`?', a: 'XOR all indices with all values, or subtract the actual sum from `n(n+1)/2`. The XOR version cannot overflow.' },
    { level: 'Easy', q: 'Why is the base of a logarithm irrelevant in Big-O?', a: 'Changing base only multiplies by a constant factor, and constants are dropped. `log2 n`, `log10 n` and `ln n` are all `O(log n)`.' },
  ],
};
