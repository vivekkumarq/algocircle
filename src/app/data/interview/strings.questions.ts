import { QuestionSet } from './question.model';

export const STRINGS_QUESTIONS: QuestionSet = {
  topic: 'strings',
  questions: [
    { level: 'Easy', q: 'Why is string concatenation in a loop dangerous?', a: 'Strings are immutable in Java and Python, so each concatenation copies everything built so far, making the loop quadratic. Use `StringBuilder`, `"".join(parts)`, or `+=` on a C++ `std::string`, which is genuinely mutable.' },
    { level: 'Easy', q: 'How do you check whether two strings are anagrams?', a: 'Compare 26-slot character counts: increment for one string, decrement for the other, then check every count is zero. `O(n)`, versus `O(n log n)` if you sort.' },
    { level: 'Medium', q: 'How do you group anagrams together?', a: 'Map every word to a canonical key — sorted characters, or the count vector rendered as a string — and group by it in a hash map. The count-vector key avoids the sort entirely.' },
    { level: 'Easy', q: 'How do you check whether a string is a palindrome?', a: 'Two pointers moving inward, comparing as they go. Decide up front whether to skip non-alphanumerics and ignore case.' },
    { level: 'Medium', q: 'Find the longest palindromic substring.', a: 'Expand around all `2n-1` centres — every character and every gap — growing outward while the characters match. `O(n²)` time, `O(1)` space.' },
    { level: 'Medium', q: 'What is the classic bug in longest palindromic substring?', a: 'Forgetting the even-length centres. Only expanding from characters silently returns `"bb"` for `"abba"`.' },
    { level: 'Hard', q: 'Can longest palindromic substring be done in `O(n)`?', a: "Yes — Manacher's algorithm reuses the mirror of previously computed palindromes. It is rarely required, but knowing linear exists is worth a sentence when you present the quadratic solution." },
    { level: 'Medium', q: 'Longest substring without repeating characters.', a: 'A sliding window with a count array. Extend right; while the entering character appears twice, shrink from the left. Record the length after the window is valid again. `O(n)`.' },
    { level: 'Medium', q: 'Longest substring with at most `k` distinct characters.', a: 'Sliding window with a frequency map; shrink from the left while the map has more than `k` entries. Remove a key when its count reaches zero, or `size()` lies.' },
    { level: 'Hard', q: 'Minimum window substring containing all characters of `t`.', a: 'Sliding window with a need-count map and a counter of how many required characters are satisfied. Expand until valid, then shrink while still valid, recording the smallest window each time before shrinking past it.' },
    { level: 'Medium', q: 'How do you check whether one string is a permutation of a substring of another?', a: 'A fixed-size sliding window of length `|p|` over `s`, comparing character counts. Comparing two 26-slot arrays is constant work, so the whole scan is `O(n)`.' },
    { level: 'Easy', q: 'Why is the naive pattern search `O(n·m)`?', a: 'It tries every start position and compares forward, and on a mismatch it discards everything it learned and restarts one character later. Text `"aaaa…a"` with pattern `"aa…ab"` hits that worst case.' },
    { level: 'Medium', q: 'How does a rolling hash work?', a: 'Treat the window as a number in base `b` modulo a large prime. Sliding one step removes the leading character and appends the new one in `O(1)`, so scanning the whole text is `O(n + m)` expected.' },
    { level: 'Medium', q: 'What must you always do after a rolling-hash match?', a: 'Verify with a direct comparison, or use two independent moduli. Equal hashes do not prove equal strings, and reporting on hash equality alone is simply wrong.' },
    { level: 'Hard', q: 'Explain the KMP prefix function.', a: '`lps[i]` is the length of the longest proper prefix of the pattern that is also a suffix of `p[0..i]`. On a mismatch you jump the pattern forward by that amount instead of restarting, so the text pointer never moves backwards. `O(n + m)`.' },
    { level: 'Hard', q: 'What else is the KMP prefix function good for?', a: 'Periodicity: the smallest repeating unit of a string is `n - lps[n-1]` when that divides `n`. It also answers shortest-append-to-make-a-palindrome.' },
    { level: 'Hard', q: 'What does the Z array store?', a: '`z[i]` is the length of the longest substring starting at `i` that matches a prefix of the string. Searching for `p` in `t` is the Z array of `p + "#" + t`, looking for positions where `z[i] == |p|`.' },
    { level: 'Medium', q: 'When would you pick hashing over KMP?', a: 'When you need to compare arbitrary substrings rather than match a single fixed pattern, or when you want the simplest correct code under time pressure. KMP wins when you need the prefix function itself or a hard worst-case guarantee.' },
    { level: 'Easy', q: 'How do you reverse the words in a sentence in place?', a: 'Reverse the whole string, then reverse each word individually. The same three-reversal trick as array rotation.' },
    { level: 'Medium', q: 'How do you check whether two strings are one edit apart?', a: 'If the lengths differ by more than one, no. Otherwise walk both with two pointers, allow a single mismatch, and advance only the longer string on a length difference of one.' },
    { level: 'Medium', q: 'Implement `atoi` — what are the edge cases?', a: 'Leading whitespace, an optional sign, digits until a non-digit, and clamping on overflow. Check overflow **before** multiplying, not after.' },
    { level: 'Medium', q: 'Compress a string like `aabcccccaaa` to `a2b1c5a3`.', a: 'One pass counting runs, appending character and count to a builder. Return the original if the compressed version is not shorter — a detail candidates often skip.' },
    { level: 'Easy', q: 'Find the first non-repeating character.', a: 'Count all characters in one pass, then scan the original string in order and return the first with a count of one. Two passes, `O(n)`.' },
    { level: 'Medium', q: 'How would you check whether two strings are isomorphic?', a: 'Two maps, one in each direction, so no two characters map to the same target. A single map wrongly accepts `"ab"` and `"aa"`.' },
    { level: 'Medium', q: 'Valid palindrome allowing one deletion.', a: 'Two pointers; at the first mismatch, check whether skipping the left character or the right character leaves a palindrome. `O(n)`.' },
    { level: 'Medium', q: 'Longest common prefix of an array of strings.', a: 'Compare characters column by column across all strings, stopping at the first mismatch or the shortest string. Sorting and comparing only the first and last also works.' },
    { level: 'Medium', q: 'How do you count palindromic substrings?', a: 'Expand around all `2n-1` centres and count every successful expansion, rather than only the longest. `O(n²)`.' },
    { level: 'Medium', q: 'What data structure suits prefix search over many words?', a: 'A trie. Lookup cost depends on the length of the query, not on how many words are stored, and words sharing a prefix share a path.' },
    { level: 'Easy', q: 'What is the difference between a substring and a subsequence?', a: 'A substring is contiguous; a subsequence keeps order but may skip characters. Substring means windows and matching algorithms; subsequence almost always means dynamic programming.' },
    { level: 'Medium', q: 'How would you detect whether one string is a rotation of another?', a: 'Check the lengths match, then test whether `b` is a substring of `a + a`. Any rotation of `a` appears inside the doubled string.' },
    { level: 'Medium', q: 'How do you handle Unicode when a problem says "characters"?', a: 'Ask. A 26-slot array assumes lowercase ASCII; for Unicode you need a hash map, and in Java you may need code points rather than `char`, since some symbols occupy two.' },
    { level: 'Medium', q: 'Why are string problems so often array problems in disguise?', a: 'A string is an array of characters, so traversal, two pointers, windows and prefix techniques all carry over. What is different is the small alphabet, which makes plain counting unusually effective.' },
  ],
};

export const HASHING_QUESTIONS: QuestionSet = {
  topic: 'hashing',
  questions: [
    { level: 'Easy', q: 'How does a hash table find a value without searching?', a: 'It hashes the key to an integer, takes that modulo the bucket count, and looks in that bucket. Computing the address replaces scanning for it.' },
    { level: 'Easy', q: 'What is a collision and how is it handled?', a: 'Two keys landing in the same bucket. Chaining stores a small list or tree there; open addressing probes the next free slot. Either way lookup stays constant on average.' },
    { level: 'Easy', q: 'Is a hash map lookup really `O(1)`?', a: 'On average, with a decent hash function and a bounded load factor. The worst case is `O(n)` when every key collides, which is why you should say "average" out loud.' },
    { level: 'Medium', q: 'What is the load factor and why does it matter?', a: 'Entries divided by buckets. Past a threshold — around 0.75 in most libraries — the table resizes and rehashes, which keeps chains short. Resizing is amortised, exactly like a doubling array.' },
    { level: 'Easy', q: 'When is an array better than a hash map?', a: 'When the keys are small dense integers: letters, digits, ages, values up to a million. An array indexed by value has no hashing cost, no collisions and better cache behaviour.' },
    { level: 'Easy', q: 'When is a tree map better than a hash map?', a: 'When you need ordering: sorted iteration, range queries, or floor and ceiling lookups. You trade `O(1)` average for a guaranteed `O(log n)`.' },
    { level: 'Medium', q: 'Explain the complement-lookup pattern.', a: 'When searching for a pair satisfying a relation, work out what the partner must be and look it up instead of scanning for it. That turns `O(n²)` into `O(n)`.' },
    { level: 'Medium', q: 'In two-sum, why check the map before inserting the current value?', a: 'Inserting first lets an element pair with itself when `target == 2·a[i]`, producing two identical indices. Checking first guarantees the indices are distinct.' },
    { level: 'Medium', q: 'How do you count subarrays with a given sum using a map?', a: 'Track the running sum; the number of subarrays ending here equals how many times `running - k` has been seen. Seed the map with `{0: 1}` for the empty prefix.' },
    { level: 'Medium', q: 'Why is `{0: 1}` needed in that map?', a: 'It represents the empty prefix, so a subarray starting at index 0 is counted. Without it those subarrays are silently missed.' },
    { level: 'Medium', q: 'How do you find the longest subarray with equal numbers of 0s and 1s?', a: 'Treat 0 as -1 and track the running sum, storing the **first** index at which each sum appeared. Two equal sums bound a balanced subarray.' },
    { level: 'Medium', q: 'What must hold between `equals` and `hashCode`?', a: 'Equal objects must have equal hash codes. The reverse is not required — unequal objects may collide. Breaking this makes entries unfindable even though they are in the table.' },
    { level: 'Medium', q: 'Why can you not use a Java array as a map key?', a: 'Arrays do not override `equals` or `hashCode`, so two arrays with identical contents are different keys. Use a list, a string, or an encoded integer instead.' },
    { level: 'Medium', q: 'How do you build a key for a grid cell?', a: '`row * cols + col` for a compact integer, or `row + "," + col` for readability. The integer form avoids allocation in hot loops.' },
    { level: 'Medium', q: 'Design a data structure with `O(1)` insert, delete and getRandom.', a: 'An array of values plus a map from value to its index. Deleting swaps the target with the last element, updates that element\'s index in the map, and pops — keeping the array dense so a random index is uniform.' },
    { level: 'Medium', q: 'How would you find the first repeating element?', a: 'One pass with a set: the first value already present is the answer. For the first **non**-repeating you need two passes, because you must know the totals before deciding.' },
    { level: 'Medium', q: 'How do you detect duplicates within a distance `k`?', a: 'A sliding window of a set: add each value, and remove `a[i-k]` as the window moves. A duplicate inside the set means one exists within distance `k`.' },
    { level: 'Medium', q: 'What is a canonical key, and where is it used?', a: 'A single representative that every member of an equivalence class produces. Sorted characters for anagrams, reduced slope for collinear points, first-occurrence pattern for isomorphic strings.' },
    { level: 'Medium', q: 'How do you find the top `k` frequent elements?', a: 'Count with a map, then either a min-heap of size `k` for `O(n log k)`, or bucket the values by frequency into an array of lists for `O(n)`.' },
    { level: 'Hard', q: 'What is a bad hash function, and how would you notice?', a: 'One that clusters keys into few buckets — for example hashing only the first character of a string. You would notice lookups degrading to linear as the data grows, with the profile spent walking chains.' },
    { level: 'Medium', q: 'Why can hashing be a security concern?', a: 'An attacker who knows the hash function can craft keys that all collide, degrading every lookup to `O(n)`. Libraries mitigate this by randomising the seed per process.' },
    { level: 'Easy', q: 'What is the difference between a hash set and a hash map?', a: 'A set stores keys only, a map stores keys with associated values. A set is usually a map with a dummy value underneath.' },
    { level: 'Medium', q: 'When should you not reach for a hash map?', a: 'When you need order, extremes, prefix queries, a guaranteed worst case, or when memory is tight — then a tree, heap, trie or plain array is the better fit.' },
    { level: 'Medium', q: 'How would you check whether two arrays are permutations of each other?', a: 'Compare their frequency maps, or sort both. The map version is `O(n)` and handles arbitrary value ranges.' },
    { level: 'Medium', q: 'What is the single instinct that hashing should give you?', a: 'When you catch yourself writing a nested loop to find something, ask what key would let you look it up instead. That question converts more quadratic solutions to linear than any other.' },
  ],
};
